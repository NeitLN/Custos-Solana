# Tiến độ thực hiện roadmap Custos

File này là **nguồn trạng thái công việc duy nhất** cho cả hai roadmap.
[BAN-GIAO.md](BAN-GIAO.md) giữ ngữ cảnh tiếp tục; không lập thêm checklist cùng mục đích.

| Roadmap | Tiền tố mã | Bảng |
|---|---|---|
| [ROADMAP-TECHNICAL-CUSTOS.md](../../ROADMAP-TECHNICAL-CUSTOS.md) — **đang thực hiện** | `TB-` | [Bảng Technical](#bảng-công-việc--roadmap-technical-tb) |
| [ROADMAP-CLAUDE.md](../../ROADMAP-CLAUDE.md) — đã đóng phần khả dụng | R/U/I/S/D/A/B/P/V/H | [Bảng roadmap trước](#bảng-công-việc--roadmap-trước) |

**Cập nhật 17/09/2026 (baseline CU-00):** HEAD `15f28b6`, **cây làm việc sạch**,
**đã push**. **840 test pass, 0 fail, 0 skipped** — đo lại trong lượt CU-00, không kế
thừa số cũ. Cổng `kiem-san-pham`: **11 đạt · 0 hỏng · 0 chưa rõ**. `nop-bai --strict`:
**11/13**, hai ô còn lại là `[ngoài]` (4 câu chưa hỏi BTC — chủ dự án đã quyết bỏ qua)
và `[người]` (chưa tạo tag).

> Con số 726 ở bản trước của dòng này là snapshot của HEAD `516f744`. Các đoạn bên dưới
> và trong BAN-GIAO.md còn ghi 726/719 là **lịch sử có nhãn**, cố ý giữ nguyên; chỉ dòng
> trạng thái hiện hành này mới được cập nhật theo lượt đo mới nhất.

Đã pull 3 commit giao diện của Duy Anh; merge sạch. 8/8 probe trình duyệt chạy lại trên
cây sạch, 10/10 biên bản a11y khớp dấu vết `f4585f12`. Lượt merge này lộ ra một phép đo
**đỏ vì lý do sai** (`scrollbar-gutter` làm `scrollWidth − clientWidth` thành âm, và
probe gọi đó là tràn ngang) và một **hồi quy thật** mà axe không thấy (vùng bấm tụt
47px → 38px, vẫn trên ngưỡng cứng WCAG nhưng dưới mốc 44px của dự án). Chi tiết và cách
chứng minh nằm ở [BAN-GIAO.md](BAN-GIAO.md).
Nền tảng: [báo cáo Codex 16/09](../review/technical/codex-20260916/BAO-CAO.md).

**Đổi track:** được BTC cho phép theo thông tin chủ dự án; chưa xác nhận biểu mẫu đã đổi hoặc cách áp mục contract. TB-H01/H02 còn WAIT_INPUT.

**Việc tiếp theo:** chủ dự án review thay đổi, commit và chạy lại cổng theo hướng dẫn trong báo cáo. TB-V01 PARTIAL vì gate nguồn sạch chưa đạt; TB-D04 là gói review cục bộ. Nhánh mở rộng có điều kiện không được tự tính DONE.

DNS đã hoạt động trong phiên này; B07 có evidence live thật, không còn chờ đổi DNS. TB-D02 có deck Technical và 11 câu phản biện; D03 có MP4 thật. Lỗi consumer tạo neo lúc ký đã sửa và có regression trước/sau.

**Bốn lỗi mới từ review 12/09**, đã tái hiện lại ở [`g00`](../review/technical/g00-20260912-024738/BAO-CAO.md)
chứ không chép kết luận: **T01** báo thành công khi xác nhận chứa lỗi thực thi ·
**T02** mất phản hồi gửi bị khẳng định là chưa gửi · **T03** chạy test làm bẩn artifact
eval · **T04** bốn phát biểu tài liệu mạnh hơn bằng chứng.

**A02 đã đóng 12/09** bằng khoá do chủ dự án cấp. Kết quả đo **không có lợi cho lớp
AI** và được giữ nguyên như vậy: xem `DON-VI-KINH-TE.md` mục 3. Câu hỏi còn lại —
*văn xuôi dễ đọc có làm người dùng hiểu đúng hơn không* — cần người thật, nay là
DEFERRED_SCOPE theo mục 1.3 của roadmap Technical.

### HEAD `780cf6d` đỏ 11 test, và năm bề mặt nói giảm về A02 — sửa 15/09

Không thuộc thẻ nào, nên ghi thẳng ở đây. Phát hiện khi rà TB-I03.

**Lỗi thứ tự trong `a0f2c91`:** `so-lieu.json` được sinh **trước** khi tài liệu cập
nhật, nên bản committed mang ảnh chụp cũ (`test.pass: 487`, `msMotLuot: 664`,
`moHinhThat: "BLOCKED_BY_SECRET"`) trong khi README/ADR/PITCH/deck/release-notes đã
nói 690 và 656. Tái hiện bằng cách khôi phục `so-lieu.json` bản HEAD rồi chạy test:
**11 bài đỏ** (số xanh của lượt tái hiện đó cố ý không ghi ra: một con số "N pass"
của một lượt dựng lại lỗi sẽ bị đọc thành số hiện tại — đúng hình dạng T04-c, và
`t04Claim.test.ts` chỉ quét `BAN-GIAO.md` nên nó không canh được dòng này).
Chú thích trong `deploy.yml` (dòng 53-63) cảnh báo đúng cái
bẫy này — *"sinh lại TRƯỚC, không phải sau"* — nhưng nó chỉ ép thứ tự **trong CI**,
không ép khi commit tay.

**Năm bề mặt nói ĐỘI CHƯA làm một việc đội ĐÃ làm.** Lượt eval với mô hình thật chạy
11/09 (`claude-haiku-4-5-20251001`, 38 mẫu, `moHinhThat.trangThai: "đã đo"`); năm chỗ
vẫn nói chưa, tất cả đều **gõ tay**, **0 guard** canh:

| Bề mặt | Sai thế nào |
|---|---|
| `apps/demo-wallet/src/SoLieu.tsx` | tiêu đề gõ cứng *"Chưa đo với mô hình thật"* đứng cạnh ô đọc dữ liệu in ra `đã đo` — trang công khai tự mâu thuẫn cách nhau hai dòng |
| `docs/AI-EVALUATION.md` mục 4.2 | giới hạn số 2 vẫn là *"Chưa đo với mô hình thật ở vòng này"*; header chỉ ghi ngày 04/09 của đường tất định |
| `README.md` bảng bốn loại bằng chứng | cột "không trả lời được" ghi *"chưa đo với mô hình thật"* |
| `scripts/kiem-nop-bai.ts` | ô checklist gõ thẳng chuỗi `"BLOCKED_BY_SECRET — cần ANTHROPIC_API_KEY"`, không đọc artifact |
| `docs/nop-bai/README.md` | mô tả script liệt kê eval như ô còn trống |

Hướng sai là **nói giảm**, không phải thổi phồng. Thể lệ phạt *"trình bày sai"* chứ
không phạt riêng một chiều, và một ô trống giả trong checklist nộp bài dẫn tới quyết
định sai của chính đội. `tao-release-notes.ts:68` đọc đúng trường này từ đầu — hai
cổng cùng đọc một artifact mà một cổng gõ tay là cách rẻ nhất để chúng nói khác nhau.

**Đã sửa:** cả năm bề mặt + guard mới `packages/core/test/evalMoHinhThat.test.ts`
(6 bài, **mutation 5 hướng đều đỏ**: tiêu đề gõ cứng lại · AI-EVALUATION quay về câu
cũ · dòng README quay về câu cũ · checklist gõ cứng lại chuỗi BLOCKED · xoá
`trangThai` khỏi artifact). Bài cuối canh chính bài kiểm: trường vắng mặt làm cả ba
bài tài liệu tự chuyển sang nhánh "chưa đo" và xanh im lặng.

**Một mốc đồng bộ gãy do TB-I02, và tôi làm gãy nó.** Sửa câu *"Tất cả nằm trong
`src/tich-hop.js`"* ở `vi-du-tich-hop/README.md` (nay sai vì hợp đồng ký ở `ky.js`)
mà không tra ai neo nó. Câu đó là **mốc regex** của `dong-bo-so-tai-lieu.mjs:243`, và
`NEO_DONG_BO` không có dòng này — nên `npm run check` vẫn xanh trong khi mốc đã chết.
Lượt `npm run so-lieu` kế tiếp ném lỗi **giữa chừng, sau khi đã ghi xong `README.md`**:
một lượt đồng bộ ghi nửa vời. Đã sửa mốc (bám `— **N dòng**` thay vì bám chữ mở đầu
câu) và thêm **ba neo** `vi-du-tich-hop/README.md` vào `NEO_DONG_BO`.

**Claim số bảy trôi:** `README.md` dòng 44 (bảng rubric Technical) giữ `inspect()`
**664 ms** trong khi sáu chỗ khác đã 656 — cùng file với dòng 93 nói 656. Dòng đó
không có mốc sync. Đã sửa số, thêm mốc và neo.

## Bảng công việc — roadmap UPDATE CUSTOS (CU-)

Nguồn thẻ: [UPDATE-CUSTOS.md](../../UPDATE-CUSTOS.md) — **đang thực hiện**.
**32 thẻ: 28 bắt buộc (CU-00…CU-27) · 4 có điều kiện (CU-O1…CU-O4).**

Trạng thái theo mục 5 của tài liệu đó: TODO · IN_PROGRESS · VERIFY · DONE · PARTIAL ·
WAIT_INPUT · DEFERRED_SCOPE · NOT_NEEDED. `UPDATE-CUSTOS.md` giữ **yêu cầu và
acceptance**; bảng này giữ **trạng thái**. Không lập bảng phần trăm thứ hai ở đó.

Đường mở đầu: `CU-00 → CU-01 → CU-02/CU-03 → CU-04 → CU-05/06/07 → CU-08 → CU-09`.

| Mã | Việc | Phụ thuộc | Trạng thái | Bằng chứng / ghi chú |
|---|---|---|---|---|
| CU-00 | Khoá baseline thực tế, dọn mâu thuẫn trạng thái | — | **DONE** | Baseline đo lại trong lượt này, không kế thừa: **730 pass · 0 fail · 0 skipped** tại HEAD `15f28b6`, cây sạch. Manifest: [`baseline.json`](../review/update-custos/). **Mâu thuẫn đã dọn:** dòng trạng thái hiện hành của TIEN-DO.md ghi 726 (snapshot của `516f744`) → sửa thành 730; các đoạn 726/719 còn lại là **lịch sử có nhãn**, cố ý giữ. **Đo được cho CU-05:** `bangChung` mới có ở **2/14 luật** (luật 11, 13) — `diff.ts:136` còn giữ `detail.includes` làm đường lui cho 12 luật kia. Đó là con số thật, không phải ước lượng. |
| CU-01 | ADR cho API mở rộng và migration | CU-00 | **PARTIAL** | [ADR-0003](../adr/0003-signer-bat-dong-bo-va-phien-ky.md) — signer bất đồng bộ. **Bốn lỗ hổng tái hiện trên consumer tham chiếu** `kySauKhiKiem`: signer reject → `daKy:true`; signer treo → `daKy:true`; gọi hai lần → signer chạy 2 lần; reject không ai bắt → **sập tiến trình**. Nguyên nhân chung: gọi `signer()` rồi `return` ở dòng sau, không await. Sửa: hàm thành `async`, **ba kết cục** (`da_ky`/`tu_choi`/`chua_ro`) vì *hết hạn chờ không chứng minh ví đã huỷ*, khoá phiên đặt **đồng bộ trước await**, đối chiếu message sau signer. Migration: 10 lời gọi trong test sang `await`, `ky.d.ts` cập nhật. Đột biến: M1 bỏ await → 11 đỏ · M2 bỏ khoá → 2 · M3 bỏ đối chiếu → 3 · **M5 sửa quá tay thành luôn-từ-chối → 6 đỏ** (đối chứng bắt được). 740 pass tại thời điểm chốt thẻ (số lịch sử). **Còn lại:** observation/effect/evidence types của mục 4.2, compatibility matrix JS/TS/browser/Node. |
| CU-02 | Session giữ đúng giao dịch và độ mới | CU-01 | **PARTIAL** | **Hai lỗ hổng đã tái hiện rồi sửa.** (1) `quaCu()` sống vô hạn ở ba ca: `msToiDa` NaN/Infinity, và `kiemLuc` ở tương lai (tuổi âm không bao giờ > hạn). Cùng loại dữ liệu xấu với `Date.parse` NaN ngay trên — chỗ kia fail-safe, chỗ này fail-open. (2) **TOCTOU**: ví đọc `tx.message.serialize()` ở chỗ dựng neo, tức SAU khi `inspect()` await; tái hiện bằng object thay `serialize` trong cửa sổ await → neo ghi đúng bản tráo và `khopNeo` trả KHỚP. Sửa: chụp `byteNay` ngay khi `txNay` sinh ra (không await ở giữa), neo bằng bytes đã chụp, so bytes cuối lượt để phát hiện tx đổi. Phép so đặt **trước** `conDung()` — đặt sau làm `c03Race` đỏ, và bài đó đỏ đúng. Đột biến 3 hướng đều đỏ; 732 pass tại thời điểm chốt thẻ (số lịch sử). **Còn lại (PARTIAL):** state machine `valid/signing/stale/consumed/cancelled/failed/unknown`, generation ID, tách thời gian đo freshness với thời gian AI — chờ CU-01 chốt contract. |
| CU-03 | Ngữ cảnh RPC, blockhash, độ đầy đủ simulation | CU-01 | **PARTIAL** | Thêm `Facts.nguCanh` (tuỳ chọn): slot, apiVersion, blockhash gốc/thay thế, compute. Trước đó **toàn bộ ngữ cảnh RPC bị nén thành một bit** `simulationOk`. **Một giả thuyết đã bị chính phép đo bác bỏ, và đó là kết quả đáng giá nhất của thẻ:** lần đo đầu cho blockhash tươi ⇒ `replacementBlockhash` giống gốc, blockhash slot −300 ⇒ khác gốc — trông như phép đo lifetime hoàn hảo. Đếm lại trên **8 lượt blockhash tươi: 6 giống, 2 khác**. Cùng điều kiện, hai kết quả ⇒ phụ thuộc thời điểm RPC xử lý, **không** đo lifetime. Suýt xây tính năng cảnh báo lên trên giả thuyết sai. Nay trường được ghi đúng nghĩa: *kết quả mô phỏng mô tả một message khác với message sẽ ký* — ngữ cảnh để trình bày, **không** phải tín hiệu rủi ro, và có bài đối chứng canh việc nó không nâng verdict. Đột biến 3 hướng đỏ (fail-open khi thiếu dữ liệu · luôn báo true · ghi ngữ cảnh sau nhánh lỗi). 749 pass tại thời điểm chốt thẻ (số lịch sử). **Còn lại:** provenance nhiều attempt, durable nonce, `minContextSlot`, genesis hash. |
| CU-04 | Evidence graph và provenance thống nhất | CU-02, CU-03 | **PARTIAL** | `packages/core/src/bang-chung.ts` — chỉ mục dữ kiện dựng từ `Facts` của **chính lượt đó**; module không import `Connection`, không `async`, nên không có đường lẫn dữ liệu lượt khác (có guard đọc mã). **Bốn nguồn** `observed`/`derived`/`missing`/`unsupported` — gộp `missing` vào `unsupported` là nói sản phẩm *không hỗ trợ* thứ nó chỉ *không đọc được lần này*. `bangChungTreo` bắt luật khai khoá không tồn tại trong Facts: **đo trên 38 fixture = 0 treo**, và phép kiểm **đỏ được** (khoá bịa bị bắt). Đột biến **5/5 đỏ**. **Bắt được lỗi của chính mình:** heredoc ghi byte NUL thật vào source ⇒ file thành **binary với Git**; đã quét toàn repo, chỉ 3 file video là binary thật. 757 pass tại thời điểm chốt thẻ (số lịch sử). **Còn lại:** nối `lenh` vào UI (CU-09), giới hạn kích thước diagnostic. |
| CU-05 | Trace cho tất cả luật đang phát cảnh báo | CU-04 | **DONE** | **2/14 → 13/14 luật** khai `bangChung`, mỗi luật đúng LOẠI định danh của nó (tokenAccount / mint / program / lookupTable / account). Luật 9 và 10 nói về *thiếu* thông tin nên trỏ tới bằng chứng của chính sự thiếu đó — chỉ mục ghi `unsupported` / `missing` kèm lý do. **Luật 14 là ngoại lệ có lý do**, không phải bỏ sót: nó báo *ví không cho biết địa chỉ nào là của bạn*, không có dữ kiện để trỏ tới. **Đường lui `detail.includes` đo được là không còn ai đi qua**: 75 hit toàn corpus, đúng 1 hit không khai (luật 14) và luật đó không nhắc địa chỉ nào. Giữ làm dự phòng cho consumer tự viết luật, ghi rõ trong mã. Đột biến 3 hướng đỏ, trong đó **khoá bịa bị bắt** — đúng thứ nghiệm thu đòi chặn. Hai guard đỏ đúng (`adrChanDoan`, `chanDoanX01`) đã sửa theo hướng **đếm từ mã** thay vì gắn cứng số. 763 pass tại thời điểm chốt thẻ (số lịch sử). |
| CU-06 | Bảng hậu quả tài sản và quyền có cấu trúc | CU-04 | **PARTIAL** | Bảng **đã có sẵn 9 nhóm** (số dư · chủ sở hữu · delegate · quyền đóng · program owner · SOL · đặt cọc · phí · chưa đọc) — không xây lại. Khoảng trống thật là `before`/`after` chỉ có chuỗi ĐÃ FORMAT; consumer phải **parse ngược chuỗi tiếng Việt** để tính toán. Thêm `DiffEntry.soLieu` tuỳ chọn: `truoc`/`sau`/`decimals`/`mint?`/`taiKhoan?`, **dạng chuỗi chứ không bigint** vì receipt (CU-11) và replay (CU-12) đi qua JSON. Dòng SOL gốc **không** mang mint wSOL (đã gộp vào chính dòng đó ⇒ cộng hai lần); dòng về QUYỀN không có `soLieu` (không có số để ghi). Đo: 56 dòng bảng, **48 có số thô**, tất cả parse và JSON hoá được. Bài mạnh nhất **format lại từ số thô rồi so với chuỗi hiển thị** — chặn rủi ro hai con số trôi khỏi nhau. Đột biến **4/4 đỏ**. 768 pass tại thời điểm chốt thẻ (số lịch sử). **Còn lại:** gross/net/fee tách riêng (cần CU-14), hiển thị địa chỉ đầy đủ trong UI. |
| CU-07 | Coverage theo năng lực | CU-04 | **PARTIAL** | `coverage.analyzed` gộp **hai điều kiện khác nhau** (`program đã xác minh` và `decode được`) vào một số. Đo corpus, 106 lệnh: **36 hiểu được · 58 quen-nhưng-chưa-đọc · 12 chương trình lạ**. Nhóm 58 là lớn nhất và rất khác nhóm 12 — hai câu, hai hành động. `chiTietCoverage()` tách ba nhóm (cộng lại = `total`, có bài canh trên toàn corpus) kèm danh sách program lạ + **có chạm tài sản người ký không**, sắp xếp chạm-trước. `coverage` cũ giữ nguyên, có bài canh `hieuDuoc === analyzed`. **Không** sinh risk score (mục 11), có guard đọc mã. **Vá một guard không đỏ được:** bài `chamTaiSan` chỉ thử lệnh chạm ở cuối nên đột biến `ix.cham` lọt; thêm ca lệnh chạm ở đầu ⇒ nay đỏ. Sửa giả định sai: `VERIFIED_PROGRAMS` là **Map** chứ không Set. 775 pass tại thời điểm chốt thẻ (số lịch sử). **Còn lại:** hiển thị chi tiết trong UI (CU-09), nối với capability matrix (CU-17). |
| CU-08 | Inspector nhận transaction bất kỳ | CU-02,03,06,07 | **DONE** | Trang `soi.html` tách khỏi ví demo. **`soi-inspector.py` 19/19 PASS trên Chromium thật**, trong đó bài quan trọng nhất đếm lời gọi mạng bằng `page.on("request")`: **đầu vào sai ⇒ 0 lời gọi RPC** (3 loại lỗi + ví sai), giao dịch Devnet thật ⇒ 3 lời gọi và thẻ kết quả hiện ra. `soiTx.ts` không import `Connection` (guard đọc mã); thứ tự kiểm rẻ→đắt (độ dài trước khi giải mã). Bốn lỗi đầu vào cho **bốn câu khác nhau**. Không nút Ký/Gửi, không hỏi khoá riêng, không lưu lịch sử. Cảnh báo riêng tư đặt **trước** ô nhập, có bài canh thứ tự DOM. Nhận ra tx **đã ký** và cảnh báo. **Tự tạo rồi sửa một lỗi:** thêm `aria-label` trùng với div của `App.tsx` ⇒ hai phần tử lồng nhau cùng tên; đổi thành "Thẻ cảnh báo Custos". 8 bài đơn vị, đột biến 5/5 đỏ. 783 pass tại thời điểm chốt thẻ (số lịch sử). |
| CU-09 | UI bấm từ cảnh báo tới dữ kiện | CU-05,06,07,08 | **DONE** | **Cổng lát cắt A — đã qua.** `Trace.tsx`: bấm mã lý do ⇒ luật, dữ kiện, **nguồn** (đã đo/suy ra/không đọc được/chưa hỗ trợ), lệnh liên quan. `chanDoan` vốn đã BẬT từ TB-X02 nhưng chưa hiển thị ở đâu — dữ liệu có sẵn, thiếu đường cho người đọc. **Điều khó hơn là thứ thẻ cấm:** không suy ra lệnh gây cảnh báo; ba chỗ nói thẳng *"Custos KHÔNG suy đoán"* thay vì để trống. `nguon` hiện to bằng chính dữ kiện vì `observed` và `missing` trông giống nhau nếu chỉ hiện khoá. Địa chỉ **đầy đủ** — rút gọn ở đây là tái tạo lỗ hổng vanity address. **Hai lần probe của tôi sai, không phải sản phẩm:** chạy `?mock=` (file mock không có `chanDoan`) và đòi trace hiện khi 0 cảnh báo (`Trace` trả `null` — cố ý, khung rỗng trông như đã kiểm mà không thấy gì). Bằng chứng: `soi-trinh-duyet` nhóm E2 **5/5** gồm bài bàn phím · `soi-inspector` **20/20**. 783 pass tại thời điểm chốt thẻ (số lịch sử). |
| CU-10 | CLI dùng được ngoài monorepo | CU-08 | **DONE** | `custos-soi --tx <base64|-> [--vi] [--rpc] [--han] [--json]`. **Exit code phân loại ENGINE, không phải quyền ký** — `0/1/2` cho safe/warning/danger, `3` lỗi đầu vào, `4` lỗi hạ tầng (tách vì hai hành động khác nhau). `--help` nói thẳng *exit 0 KHÔNG có nghĩa ký được*, có bài canh. `--json` ba trường tách riêng, `policyDecision: null` cho tới CU-18; stdout sạch, chẩn đoán vào stderr. **Lỗi đã đo rồi sửa:** `Buffer.from(s,"base64")` không bao giờ ném — nó bỏ ký tự lạ, nên `rác!!!` báo nhầm thành *không phải giao dịch Solana*; kiểm hình dạng base64 trước khi giải. **Nghiệm thu chạy thật:** cài 3 tarball vào thư mục trống → `node dist/cli.js --help` exit 0 **không cờ bóc kiểu** · đầu vào sai exit 3 stdout trống · `import` subpath `./cli` từ JS thuần. Thêm bước 6/6 vào `thu-goi` để tarball luôn được kiểm; đột biến xoá `bin` ⇒ **đỏ đúng câu**. 8 bài mới, phần lớn spawn tiến trình thật. 799 pass tại thời điểm chốt thẻ (số lịch sử). |
| CU-11 | Export receipt và redaction | CU-04,08,10 | **PARTIAL** | `packages/core/src/receipt.ts` — hai chế độ `rieng`/`chiaSe`. **Bản chia sẻ KHÔNG mang `Facts`**, và đó là hệ quả cố ý chứ không phải thiếu sót: `Facts` chứa toàn bộ trạng thái tài khoản đã đọc. Biên lai tự ghi `khongReplayDuoc` kèm lý do để CU-12 từ chối replay thay vì bịa `Facts`. **Quy tắc che chuyển từ ví lên core** (`che-nhay-cam.ts`) — hai bộ quy tắc song song thì vá một bên, bên kia vẫn hở. Corpus secret giả **5 mẫu × 2 chế độ = 10 ca, 0 lọt**: RPC credential (Helius/QuickNode/Alchemy) và đường dẫn máy lộ tên người dùng. Đối chứng canh secret nằm SÂU trong `Facts`, và canh biên lai KHÔNG bị xoá trắng (che sạch mọi chuỗi cũng làm bài kia xanh). **Một lỗi tự gây rồi tự bắt, chỉ trình duyệt mới thấy:** bản đầu dùng `node:crypto` ⇒ Vite externalize ⇒ **trắng cả hai trang**, trong khi 854 test vẫn xanh vì test chạy trên Node. Thay bằng `sha256.ts` tự viết, đồng bộ, đối chiếu **vector chuẩn NIST + `node:crypto` trên 131 độ dài** quanh ranh giới khối 55/56/63/64. Guard đọc mã chặn `node:` quay lại barrel. Đột biến **3/3 đỏ** (tắt che · `chiaSe` vẫn mang Facts · hash hằng số). CLI có `--receipt chiaSe`; `--receipt rieng` **bị từ chối kèm lý do, exit 3, stdout sạch** vì `inspect()` không trả `Facts` — không nới hợp đồng đã đóng băng chỉ để tiện cho CLI. Đã chạy thật trên fixture R01-pos: `level danger`, 2 mã lý do, hash 64 ký tự, 4 câu ranh giới. 862 pass. **Còn lại:** bản đọc được (Markdown/HTML), preview trước export, xuất từ UI.  |
| CU-12 | Replay offline có nhãn | CU-11 | **PARTIAL** | `packages/core/src/replay.ts` — **không có bộ luật riêng**: gọi đúng `danhGia` và `dungBangChenhLech` mà `inspect()` gọi, và đọc `facts.coverage` thay vì tự tính (bản đầu tôi tự ghép ba số từ `chiTietCoverage` — một phép tính thứ hai là một phép tính sẽ trôi khỏi bản gốc). Guard đọc mã chặn `Connection`/`fetch`/`await` quay lại file, có bỏ chú thích trước khi tìm để không khớp phải chính chú thích của mình. **Trả CẢ HAI verdict** — `nguyenVan` (lúc xuất) và `chayLai` (engine hôm nay) — kèm `engineDaDoi`, thay vì chọn một rồi im lặng. `toanVen` tách khỏi `ok`: biên lai bị sửa vẫn ĐỌC được, chỉ là biết nó đã bị sửa. Bản `chiaSe` **bị từ chối replay**, không bịa Facts rỗng. Nhãn *Dữ liệu ghi lại* đi cùng kết quả kèm thời điểm, không nằm trong README. Đột biến **3/3 đỏ**, trong đó **M3 đắt nhất**: cho `khacNhau()` so cả `explanation` ⇒ `engineDaDoi` bật vĩnh viễn (L3 không chạy lúc replay) ⇒ **3 bài đỏ**, đúng thứ đối chứng sinh ra để canh. Chạy lại **toàn bộ mẫu corpus có facts**, không chỉ một. 872 pass. **Còn lại:** nối vào UI (màn kết quả), CLI `--replay`, kiểm size/version của payload lớn.  |
| CU-13 | Registry decoder có phạm vi năng lực | CU-04,07 | **PARTIAL** | `nang-luc.ts` — **ba mức** `khongBiet`/`docDuocTen`/`hieuHauQua`. Đo được, sinh từ registry chứ không gõ tay: **13 program · 377 lệnh đọc được TÊN** nhưng chỉ **2 program · 8 lệnh hiểu được HẬU QUẢ**. Khoảng cách 377↔8 là toàn bộ nội dung thẻ — đọc được `"swap"` từ IDL Jupiter không có nghĩa biết swap lấy bao nhiêu của ai. Program lớn nhất (77 lệnh) ở mức `docDuocTen`: đọc tên cả 77 lệnh, **hiểu hậu quả 0 lệnh**. `hieuHauQua` chỉ cho SPL Token/Token-2022 (bố cục account cố định). **Hai lỗi tự bắt:** nhận diện bằng `soLenh === 44` (sai im lặng khi bảng đổi) ⇒ đổi sang địa chỉ program; và guard đọc mã **không tách chú thích** nên khớp phải câu văn kể lại lỗi cũ — mắc lại đúng bẫy đã ghi trong bàn giao. Đột biến **5/5 đỏ**, gồm M2 *gán `hieuHauQua` cho program chỉ có IDL* (đúng thứ thẻ cấm). 816 pass tại thời điểm chốt thẻ (số lịch sử). **Còn lại:** interface decoder chuẩn hoá, bounded parser, đối chứng prefix trùng. |
| CU-14 | Token-2022 transfer fee và amount thực nhận | CU-03,06,13 | **PARTIAL** | `packages/core/src/phi-token.ts`. **Công thức lấy từ mã nguồn protocol, không tự suy:** trang chính thức `solana.com/docs/tokens/extensions/transfer-fees` nêu hai tham số và ví dụ (200 token · 150 bps · cap 10 ⇒ phí 3) nhưng **không nói rounding** — đã tra `calculate_fee` của token-2022 và lấy đúng ba chi tiết quyết định: **`ceil_div` làm tròn LÊN** (không xuống), **cap áp SAU CÙNG** qua `cmp::min`, và **thoát sớm** khi bps=0 hoặc amount=0. Test vector **tính tay** từ công thức, ghi phép tính vào chú thích — thẻ cấm đích danh dùng implementation làm oracle, vì hàm sai thì vector chép từ nó cũng sai y hệt. Ba số **tách riêng không có trường tổng** (`phiToken` là token, phí mạng là lamport — cộng hai đơn vị khác nhau là tạo số vô nghĩa). Thiếu config hoặc thiếu **epoch** ⇒ `khongBiet`, không suy từ ký hiệu token và không đoán bừa bản nào đang hiệu lực. Tham số sai ⇒ `null` chứ không 0, vì 0 hiện thành *không mất phí* — một câu sai về tiền. **KHÔNG sinh `level`, không có mã lý do** — transfer fee là năng lực hợp lệ, gắn cờ vì token thu phí là cách nhanh nhất tạo false positive (mục 06); có bài đọc chính kiểu trả về để canh. Đột biến **4/4 đỏ** đúng ba chi tiết dễ sai nhất. 934 pass. **Còn lại:** đọc `getTransferFeeConfig` thật trong L1, số `quanSat` từ simulation, withheld state.  |
| CU-15 | Quyền Token-2022 và transfer hook | CU-05,06,13 | **PARTIAL** | `packages/core/src/quyen-token.ts` — **không thêm luật, không sinh `level`** (L2 đã có luật permanent delegate và transfer hook từ trước, và đã phân biệt *có quyền* với *dùng quyền*). File này trả lời ba câu mà một dòng cảnh báo không trả lời được: ai có quyền gì · dữ kiện đọc ở **mint hay token account** · quyền đó **MỚI hay có sẵn**. Câu thứ ba là ranh giới chính: *"token này có permanent delegate"* mô tả một token, *"giao dịch này vừa trao permanent delegate"* tố cáo một hành vi — trộn hai câu là buộc tội sai và là cách nhanh nhất tạo false positive (mục 06). `moiTrongGiaoDich` tính từ dữ kiện đo được (`…Before` ≠ `…After`), không từ suy đoán. Hook chưa quan sát được ⇒ `doDayDu: khuyet` chứ **không** diễn giải thành *không có hậu quả*. **Không blacklist theo tên program** — có guard đọc mã, và đột biến M4 cài `startsWith("Safe")` bị bắt. Đột biến **4/4 đỏ**. Chạy trên toàn corpus: mọi quyền nêu ra đều trỏ tới đối tượng có thật trong Facts. 944 pass. **Còn lại:** đọc extra accounts của hook, nối vào UI, ca đối chứng hành vi đáng ngờ cho từng nhóm.  |
| CU-16 | SOL, phí và vòng đời account | CU-03,06 | **PARTIAL** | `chiTietSol()` tách `roi` thành **phí · đặt cọc · phần còn lại** — ba câu khác nhau: mất thật · **lấy lại được** · chưa biết đi đâu. **Fee payer ≠ ví được bảo vệ:** người trả phí là `nguoiKy[0]`, khác `signer` thì phí **không** tính cho người dùng; không biết ai trả thì **tính vào người dùng** (fail-safe nghiêng phía thận trọng). `nguoiDungTraPhi` có ba giá trị `true/false/null`. `chuaGiaiThich` giữ lại để bất biến `phi + datCoc + conLai = roi` **kiểm được**, chạy trên toàn corpus. `conLai` không gọi là *chuyển đi* — Custos chỉ biết nó rời ví. **Một phép đo suýt dẫn sai:** corpus cho *38/38 chưa giải thích được*, đọc kỹ thì fixture **không có** trường `phiUocTinh` (tạo trước khi trường đó tồn tại). Đột biến **4/4 đỏ**. 808 pass tại thời điểm chốt thẻ (số lịch sử). **Còn lại:** refund khi đóng account, priority fee tách riêng, wrap/unwrap wSOL. |
| CU-17 | Capability matrix | CU-07,13,14,15,16 | **PARTIAL** | `docs/MA-TRAN-NANG-LUC.md` **sinh từ registry** (`npm run ma-tran-nang-luc`). **Sửa một lệch claim đã tồn tại:** `DAC-TA-CORE.md` liệt kê **6 program** và có **SPL Memo**; registry thật **13 program**, **không có Memo** — sai cả hai hướng (thiếu 7 cái có thật, thừa 1 cái không có). Danh sách gõ tay lúc registry còn 6 rồi không ai sửa; cùng hình dạng lỗi số test ở sáu tài liệu. Bảng tự bác bỏ ba cách đọc sai (*known* ≠ *trusted* · mô tả Custos không mô tả giao dịch · không viết *đã hỗ trợ Jupiter*) và nói người dùng **làm được gì** với từng gap. Đột biến **4/4 đỏ**, bắt **cả hai chiều** lệch — bài khớp từng địa chỉ, vì bảng đúng số mà sai người vẫn sai. 824 pass tại thời điểm chốt thẻ (số lịch sử). **Còn lại:** nối vào UI và CLI (cần CU-14/15 cho Token-2022). |
| CU-18 | Policy của ví tách khỏi engine | CU-02,07,17 | **PARTIAL** | `packages/core/src/policy.ts` — evaluator thuần, `allow`/`review`/`block` kèm phiên bản và mã lý do **riêng không gian tên** `POLICY__*`. **Bất biến chính, cùng họ với quy tắc bất đối xứng của `expectedAction`: policy CHỈ được thận trọng hơn.** Engine `danger` ⇒ không đường nào thành `allow`; nếu policy nới được thì kẻ tấn công chỉ cần làm ví nạp profile dễ dãi và cả lớp phát hiện thành trang trí. `capDo()` cưỡng chế ở MỘT chỗ, và bài canh chạy **toàn bộ truth table** (3 level × 3 phiên × 3 coverage × 3 profile) chứ không vài ca chọn lọc. Profile là **tham số riêng**, không nằm trong bối cảnh — guard đọc mã chặn `facts`/`instructions`/`VersionedTransaction` để dApp không tự khai profile. Phiên `null` = *không biết* ⇒ ít nhất `review`. Đột biến **4/4 đỏ**, trong đó **M4 chặn-tất-cả làm 3 bài đỏ** — đối chứng bắt được an toàn giả. CLI có `--policy mac-dinh|chat`; tên gõ nhầm **bị từ chối** thay vì im lặng lui về profile lỏng. **`policyDecision` vẫn `null` khi vắng cờ** — `null` (chưa ai xét) khác hẳn `allow` (đã có người cho phép). Có bài canh **exit code KHÔNG đổi theo policy**: exit code nói engine thấy gì, policy nói ví cho phép gì. 888 pass. **Còn lại:** nối vào UI ví, allowlist có giới hạn mục đích, lưu profile qua nhiều phiên.  |
| CU-19 | Adapter ví và signer bất đồng bộ | CU-02,03,18 | **PARTIAL** | `vi-du-tich-hop/src/tich-hop-policy.js` — vòng đời đầy đủ request → inspect → **policy CU-18** → consent → signer. `tich-hop.js` cũ **giữ nguyên**: nó có luật `ky/hoi/chan` gõ cứng (một bộ policy thứ hai), nhưng con số *"30 dòng tích hợp"* đếm đúng nó và được công bố ở 6 chỗ — đã kiểm lại sau khi thêm: **vẫn 30**, 8/8 kịch bản vẫn xanh. Ví giả kiểm **mọi nhánh**, đếm lượt gọi signer (cách duy nhất chứng minh *không gọi ở nhánh chặn*): danger ⇒ 0 lần · review không ai đồng ý ⇒ 0 · người dùng từ chối ⇒ 0 · profile chặt ⇒ 0. `khong_ro` KHÔNG gộp vào thất bại — ví có thể đã ký mà phản hồi chưa về, gộp rồi cho ký lại là tạo hai chữ ký cho một ý định. **Hai lỗi của chính tôi, cả hai do đo mới thấy:** (1) bài *signer trả tx khác* đỏ với `da_ky`, suýt đi sửa `ky.js` — đo ra hai `tx()` không tham số cho **0 byte khác nhau**, sản phẩm đúng, ca kiểm sai; (2) **đột biến M1 bỏ hẳn nhánh `block` của policy mà 12/12 vẫn xanh** — hoá ra `danger` rơi xuống `review`, không ai đồng ý, nên `kySauKhiKiem` chặn ở LỚP SAU. Phòng thủ nhiều lớp là tin tốt, nhưng bài không phân biệt được hai lớp thì lớp policy chết hẳn cũng không ai biết; nay bài đòi đúng `lyDo === "policy_chan"`, và có bài riêng đóng cọc lớp sau. Đột biến **4/4 đỏ** sau khi sửa. 901 pass. **Còn lại:** trạng thái submitted/confirmed (cần đường gửi thật), nối vào UI ví, consumer ngoài repo.  |
| CU-20 | So sánh hai transaction/kết quả | CU-06,08,11 | **PARTIAL** | `packages/core/src/so-sanh.ts` — **hai loại tách hẳn**: `cauTruc` (tất định, khác byte ⇒ giao dịch thật sự khác) và `quanSat` (hai lần đọc, khác nhau có thể chỉ vì trạng thái chuỗi đổi). **Ranh giới quan trọng nhất: state drift KHÔNG tự chứng minh dApp tráo giao dịch** — câu của `quanSat` nói thẳng điều đó, và đột biến **M1 đổi câu thành buộc tội làm bài đỏ ngay**, tức ranh giới ngôn ngữ được cưỡng chế bằng test chứ không bằng lời dặn. Khớp dòng bằng **định danh bền** `programId#thứ tự trong program` (M2 đổi sang vị trí tuyệt đối ⇒ đỏ) và bằng **địa chỉ chứ không ký hiệu token** (M3 ⇒ đỏ; ký hiệu do metadata đặt, mà metadata là thứ kẻ tấn công điều khiển). Ca vanity cùng 4 đầu + 4 cuối vẫn bị bắt vì so địa chỉ ĐẦY ĐỦ. Đột biến **4/4 đỏ**. **Hai lỗi tự bắt:** (1) `a.nguoiKy.length` ném trên fixture cũ không có trường đó — cùng lớp với `phiUocTinh` ở CU-11, corpus là dữ liệu thật và dữ liệu thật có lịch sử; (2) lệnh mới chỉ báo `"có"` thay vì nêu nó LÀ GÌ — *"có thêm một lệnh"* kém xa *"có thêm SetAuthority → X"*. 910 pass. **Còn lại:** so blockhash/ALT/fee (cần `nguCanh` của CU-03), UI hai cột, session riêng cho B.  |
| CU-21 | Batch review có giới hạn đúng | CU-08,18,20 | **PARTIAL** | `packages/core/src/batch.ts` — **không có badge xanh tổng**. Mức của lô = mức NẶNG NHẤT, và **một** phần tử chưa kiểm được đủ làm cả lô `khong_ket_luan_duoc`; đột biến M1 (bỏ qua phần tử chưa kiểm) làm **3 bài đỏ**, nhiều nhất trong 5 — đúng thứ nguy hiểm nhất của thẻ được canh chặt nhất. Mỗi phần tử giữ `viTri`, kết quả và **policy riêng** (không dùng chung một quyết định). B phụ thuộc A ⇒ `phu_thuoc_chua_giai_duoc` kèm số hiệu giao dịch phụ thuộc, **không sửa thành pass**, và `policy: null` vì chưa có gì để quyết. Mọi câu tổng — kể cả câu tốt nhất — đều nói rõ **mô phỏng ĐỘC LẬP**, và câu xanh kèm *TRONG PHẠM VI*. `chuKyLo()` băm theo thứ tự nên đổi thứ tự / thêm / bớt đều làm đồng thuận cũ hết hiệu lực (M4 sắp xếp trước khi băm ⇒ đỏ). Cluster khai lệch nhau ⇒ **từ chối cả lô**, vì kết quả hai cluster không so được với nhau. Giới hạn 20 tx / 20×1232 byte, có đối chứng *đúng giới hạn thì phải nhận* để tránh off-by-one chặn nhầm. Hàm **không async, không chạm mạng**: người gọi tự chạy `inspect()` rồi đưa kết quả vào, nên huỷ giữa chừng và chạy song song là việc của họ. Đột biến **5/5 đỏ**. 924 pass. **Còn lại:** UI danh sách, nối với `soSanhCauTruc` để phát hiện tx trùng trong lô.  |
| CU-22 | Abort, retry và cache đúng ngữ cảnh | CU-02,03 | **PARTIAL** | `coHan()` tự khai *"chỉ ngừng CHỜ"* — mỗi lượt quá hạn để lại request vẫn chạy. **Đo trước khi xây:** `Connection` nhận `fetch` tuỳ chỉnh (có) · web3.js tự truyền signal (**không**) · tự gắn signal thì abort **cắt được thật** (AbortError). `huy.ts` gắn AbortSignal cho mọi request cùng Connection; signal người gọi được **kết hợp** qua `AbortSignal.any`, không ghi đè; `soRequest()` đếm được; `laHuy()` tách huỷ khỏi lỗi. **Lỗi do chép theo trí nhớ:** regex viết *"The operation was aborted"* nhưng Node 24 ném *"This"* — test đỏ ngay. Nối vào Inspector: nút Huỷ cắt thật. Đột biến **5/5 đỏ** gồm M3 "luôn abort". `soi-inspector` nhóm F **6/6** với RPC làm chậm 8s: kết quả về muộn không ghi đè trạng thái huỷ. Tổng probe **26/26**. 791 pass tại thời điểm chốt thẻ (số lịch sử). **Còn lại:** retry bounded, cache theo cluster/program, deadline bao cả orchestration. |
| CU-23 | Đo hiệu năng gắn với chức năng mới | CU-09,12,22 | **PARTIAL** | `npm run do-hieu-nang` — đo 9 phép tính tất định của CU-11/12/14/15/18/20/21 trên fixture thật, **n=195 warm** sau khi bỏ 5 lượt đầu. **Script TỪ CHỐI in p95 khi n < 20**: ở đó `ceil(0,95·n)−1` trỏ đúng vào phần tử lớn nhất, nên in ra dưới tên *p95* là trình bày cực trị của mẫu nhỏ như một thống kê — thẻ cấm đích danh. Số đo (trung vị): L2 `danhGia` **0,005 ms** · biên lai 0,089 · replay 0,117 · phí token ~0 · tóm tắt quyền ~0 · policy ~0 · so sánh 0,001 · lô 20 giao dịch 0,032. **Một điểm ngoại lai đã truy tới cùng:** CU-12 cold 9,95 ms vs trung vị 0,117 — gấp 85 lần; đo từng lượt cho thấy lượt 0 = 10,4 ms rồi lượt 1 đã về 0,46 và ổn định ⇒ **JIT warm-up**, không phải chi phí tăng dần hay rò rỉ. Bảng tự khai phạm vi *không chạm mạng* và trỏ tới `thu-tich-hop:devnet` cho độ trễ thật (~600 ms), vì một bảng 0,1 ms không kèm phạm vi sẽ bị đọc thành *Custos chạy trong 0,1 ms*. Guard canh **chính phép đo** chứ không canh con số (ngưỡng thời gian trong test tất định là nguồn flaky). **Một guard xanh vì lý do sai, tự bắt:** bài đếm luật dùng regex bắt nhầm `LUAT_DO` (4 luật) rồi nuốt chú thích, đếm ra **28** ⇒ 28 ≥ 14 nên xanh mà không canh gì; sửa thành `import { LUAT }` rồi `LUAT.length === 14`, đột biến bỏ 1 luật ⇒ đỏ đúng câu. 949 pass. **Còn lại:** đo click→result và bundle size trên trình duyệt, ngân sách DOM cho evidence lớn.  |
| CU-24 | Corpus mở rộng và kiểm đối kháng | CU-04 | **PARTIAL** | **Tìm ra lỗi thật: 7 fixture đối chứng bị gán nhãn NGƯỢC.** `R03/05/06/07/09/10/11-neg` ghi `cuc: "duong"` trong khi `kyVong.khongCoMa` nói rõ là đối chứng — hai trường cùng bản ghi mâu thuẫn. **Test vẫn xanh** vì chúng đọc `kyVong`, không đọc `cuc`; hậu quả là **manifest nói dối về cấu tạo corpus** (báo *7 luật không có ca đối chứng* trong khi cả 7 đều có). Sau sửa: **14/14 luật có cả hai cực**, 824 test vẫn xanh ⇒ sửa nhãn không đổi hành vi. **Cách tìm ra:** phép đếm của tôi (theo `cuc`) mâu thuẫn với bài `capLuat` đang xanh (theo `kyVong`) — hai điều không thể cùng đúng. 4 bài mới; đột biến trả lại nhãn sai cho **một** fixture ⇒ **3 bài đỏ**. 828 pass tại thời điểm chốt thẻ (số lịch sử). **Còn lại:** thêm `feature`/`lyDoNhan`/`split` vào manifest, holdout, ca malformed và missing-data cho từng family. |
| CU-25 | Contract tests, package, tài liệu tích hợp | CU-10,11,18,19,21,22 | **PARTIAL** | `thu-goi` thêm **bước 7/7** chạy trên chính tarball: (a) 14 API mới của CU-11/12/14/18/20/21 gọi được từ ngoài monorepo **và giữ đúng bất biến** (bản chia sẻ bị từ chối replay · `danger` ⇒ `block` · phí 200/150bps/cap10 = 3 · một tx danger ⇒ cả lô không xanh); (b) **quét mọi file dist tìm import Node-only** — đúng lỗi đã xảy ra ở CU-11 khi `node:crypto` làm trắng cả hai trang trong khi 854 test vẫn xanh. `cli.js` được miễn vì nó không vào bundle trình duyệt. **Guard đỏ vì lý do sai, tự bắt:** bản đầu quét văn bản thô và báo `neo.js`/`receipt.js`/`sha256.js` đều kéo `node:crypto` — cả ba chỉ NHẮC tên đó trong chú thích kể lại chính lỗi này; sửa thành bỏ chú thích trước khi tìm, rồi **đột biến thêm import THẬT ⇒ đỏ đúng file**. Một lỗi nữa: `chay()` trả THẲNG chuỗi stdout và tự ném khi fail, không phải `{stdout, stderr}` — bản đầu in `undefinedundefined`, lỗi có thật nhưng thông báo rỗng. README có mục mới cho biên lai/replay/policy/lô, và **4 bài chạy đúng đoạn mã in trong đó** — gồm bài neo ba câu phủ định (*hash không phải chữ ký* · *che ≠ ẩn danh* · *policy chỉ thận trọng hơn*) vào mã nguồn, vì chúng đúng lúc viết và không có gì nhắc khi sản phẩm đổi. 953 pass. **Còn lại:** kiểm build TS và browser bundle thật của consumer ngoài, migration cho schema biên lai.  |
| CU-26 | Hoàn thiện UX và tài liệu sử dụng | CU-09,12,17,20,21 | **PARTIAL** | **Nút *Xuất biên lai* trên Inspector**, chỉ chế độ `chiaSe` vì `inspect()` không trả `Facts` ra ngoài — nói thẳng điều đó thay vì xuất một file trông đầy đủ mà rỗng ruột. Tải bằng Blob + object URL, **không gửi đi đâu**. Kiểm trên Chromium thật **19/19**, gồm tải file thật rồi đọc nội dung: `cheDo: chiaSe` · KHÔNG kèm `facts` · tự khai `khongReplayDuoc` · hash 64 ký tự · `level: danger` đến từ L2 · 4 câu ranh giới · **không lộ đường dẫn máy, không lộ api-key** · vùng bấm 44px · 375px không tràn ngang · 0 lỗi console. Chữ giải thích nói rõ ba điều: không chạy lại được · đã che thông tin đăng nhập · **hash không phải chữ ký**. `soi-inspector` và `soi-trinh-duyet` (axe) chạy lại sau khi đổi UI: **TẤT CẢ PASS**, không vi phạm mới. Dùng lại hệ CSS sẵn có, không redesign. 953 pass. **Còn lại:** nạp biên lai để replay trong UI, màn so sánh A/B và màn lô — **thuộc vai B**, và phạm vi này được chọn có chủ ý để không sửa chéo sát hạn nộp.  |
| CU-27 | Nghiệm thu bản nâng cấp và demo | CU-00…CU-26 | **PARTIAL** | [`docs/NGHIEM-THU-CU-27.md`](../NGHIEM-THU-CU-27.md) — **bốn nhóm kết luận tách riêng**, vì trộn chúng là cách một dự án tự khen mình. Chạy lại toàn bộ ma trận trên bản ứng viên tại HEAD `11380bf`: 953 test · tích hợp tất định 14/14 · gói 7/7 kèm 10/10 bẫy · **live Devnet PASS** · đối kháng AI `soSai: 0` · ranh giới L2/L3 13/13 · axe 0 vi phạm · Inspector Chromium PASS · cổng sản phẩm **10 đạt · 0 chưa rõ**. Số liệu lấy từ nguồn sinh, không gõ tay trên deck. **Nói thẳng phần yếu:** ô bằng chứng người dùng vẫn TRỐNG (0 phỏng vấn người mua · 0 bên thứ ba tích hợp · usability vòng 2 chưa chạy · eval mô hình thật `BLOCKED_BY_SECRET`), và thẻ dặn đúng điều này — **không nâng điểm vì hoàn thành nhiều thẻ**. Probe tự động chứng minh giao diện *chạy được*, không chứng minh người thật *hiểu được*. Mục 6 ghi lại bốn lỗi chỉ lộ ra khi đo, cả bốn đều từng có test xanh bao quanh. **Còn lại:** release tag và 4 câu hỏi BTC — `TB-H01`/`TB-H02`, chỉ chủ dự án làm được.  |

## Điều chỉnh UI — nhận diện xanh ngọc (UIR-00 → UIR-05)

Theo `docs/DIEU-CHINH-UI-CUSTOS.md`. Tài liệu này **thay phần art direction** của
đặc tả website trước; provenance, VI/EN, route và accessibility vẫn giữ.

| Gói | Trạng thái | Bằng chứng |
|---|---|---|
| UIR-00 | **DONE** | Regression mới `soi-layout-landing.py` **đỏ 8/30 trước khi sửa**: 320px VI tràn 38px · **320px EN 55px** · **360px EN 15px** (hai ca sau tài liệu chưa ghi) · chip đè title 6,25px · mép container `[0, 124, 334]` |
| UIR-01 | **DONE** | `brand-tokens.css` chỉ chứa biến, tên theo VAI TRÒ. Bỏ hard shadow/backplate/chip/dải lime. Contrast đo thật 15/16 đạt; token control border đầu tiên **2,17:1 — không đạt**, đổi sang `#6E8F88` được 3,53:1 |
| UIR-02 | **DONE** | A/B thành bảng hai cột đồng thời; evidence có nút riêng từng ca và hiện tên ca. Mobile hai tóm tắt + dòng đối chiếu cố định. Reset list sửa bằng `:where()`, không vá một dải |
| UIR-03 | **DONE** | Developer nền xanh đậm; FAQ hai cột; `theme-color` trắng; social preview dựng lại. Test ghim `#17132A` đã sửa, thêm 2 guard chống màu cũ quay lại |
| UIR-04 | **PARTIAL** | `--color-nhan` → teal sau khi phân loại 13 nơi dùng (đều brand/action). `--color-thuong` **GIỮ NGUYÊN** vì là semantic verdict. Đã mở idle/danger/safe/Inspector, 0 pageerror. **Chưa mở** trạng thái thiếu coverage và lỗi RPC |
| UIR-05 | **DONE** | Build + preview `/Custos-Solana/`; probe **77/77** và **30/30**; `soi-trinh-duyet`/`soi-inspector`/`soi-vung-bam` PASS; 968 test. Bàn giao: [`ui-teal-20260919`](../review/ui-teal-20260919/BAO-CAO.md) |

**Bốn lỗi của chính lượt này, đã ghi trong bàn giao:** guard overlap bỏ sót chip vì
lọc nhầm · CTA 46px thay vì 48px · guard màu cũ khớp phải chú thích của chính nó
(lần thứ tư) · social preview trỏ cổng server đã tắt.

## Website giới thiệu (WEB-00 → WEB-07)

Thực thi theo `docs/WEBSITE-CUSTOS-DESIGN-VA-TRIEN-KHAI.md`. Đây là **trang giới
thiệu**, không phải thẻ CU/DW — không đánh dấu CU nào DONE từ việc viết copy web.

| Gói | Trạng thái | Bằng chứng |
|---|---|---|
| WEB-00 | **DONE** | Bản đồ `nội dung → nguồn → link đích` lập từ repo thật: A/B lấy từ `scenarios.json` (commit `dd7e776`, `broadcast: false`); 4 link ngoài đối chiếu file có thật; **gap ghi rõ** — npm publish `0.1.1`, source `0.2.0`, nên landing KHÔNG hứa npm có mọi feature HEAD |
| WEB-01 | **DONE** | `gioi-thieu.html` + `src/landing/` + `landing.css` (1426 dòng, token `--landing-*`). CSS **cô lập**: không `@import "tailwindcss"`, không import `style.css` của ví. Vite input mới; `isPreview`/`css.postcss`/`buffer` giữ nguyên |
| WEB-02 | **DONE** | 10 section theo mục 5–6, copy VI thật. Anchor, FAQ `<details>`, menu mobile, footer. **0 `href="#"` rỗng** (probe kiểm) |
| WEB-03 | **DONE** | `sample.ts` rút gọn từ artifact, có provenance. **6 bài test neo từng giá trị vào nguồn**. Ca A `doiChu: null` — không suy owner từ việc thiếu diff. Badge mẫu luôn hiện. **0 request RPC khi mở trang** (đo bằng `page.on("request")`) |
| WEB-04 | **DONE** | CTA nối `BASE_URL`; 3 route nội bộ + 4 link ngoài kiểm trong probe. Code excerpt dùng **API hiện hành** (`inspect`), dừng ở chỗ đọc kết quả, chú thích chỉ sang hướng dẫn consumer |
| WEB-05 | **DONE** | EN đầy đủ (kiểu `NoiDung` cưỡng chế). 5 viewport 320→1920 **0 tràn ngang**; keyboard, Escape trả focus, skip link; **axe 0 vi phạm** cả VI/EN |
| WEB-06 | **DONE** | Font self-host **116,5 KB** (8 WOFF2, giữ subset vietnamese), `asset-sources.md` ghi license OFL. **Mascot 363,8 → 9,8 KB**; tổng tải **480,4 → 126,3 KB**. Social preview 1200×630 dựng bằng code. JS landing **11,3 KB gzip** |
| WEB-07 | **PARTIAL** | Build + preview dưới `/Custos-Solana/` **MIME đúng** (JS `text/javascript`, không phải `index.html`). Probe **75/75**; ví/Inspector/số liệu **vẫn chạy**; `soi-trinh-duyet` và `soi-inspector` **PASS**. **CHƯA deploy** — bản đã build và kiểm local, không phải "website đã online" |

**Chưa làm, có chủ ý:** live A/B nhúng trong landing (mục 7.4 — nâng cấp có điều
kiện, đã chọn link sang app làm mặc định); prerender HTML EN riêng cho crawler
(mục 13 — đổi ngôn ngữ bằng JS không bảo đảm crawler đọc metadata EN, giới hạn đã
ghi trong mã); canonical/`og:url` (URL production chưa xác nhận đã deploy).

| CU-O1 | Hỏi đáp về bằng chứng giao dịch | CU-05,11,12 | DEFERRED_SCOPE | Nhánh có điều kiện — chưa mở. |
| CU-O2 | Trang rà quyền của một ví, chỉ đọc | CU-15,17,22 | DEFERRED_SCOPE | Nhánh có điều kiện — chưa mở. |
| CU-O3 | Semantic adapter cho một protocol | CU-13,17,24 | DEFERRED_SCOPE | Nhánh có điều kiện — chưa mở. |
| CU-O4 | Dịch vụ HTTP cho tích hợp ngoài trình duyệt | CU-10,11,22,25 | DEFERRED_SCOPE | Nhánh có điều kiện — chưa mở. |

---

## Bảng công việc — roadmap Technical (TB-)

Nguồn thẻ: [ROADMAP-TECHNICAL-CUSTOS.md](../../ROADMAP-TECHNICAL-CUSTOS.md).
**40 thẻ: 34 bắt buộc · 4 có điều kiện (L02, O01–O03) · 2 cần bên ngoài (H01–H02).**

Trạng thái theo mục 6.1 của roadmap đó: TODO · DOING · VERIFY · DONE · WAIT_INPUT ·
BLOCKED_TECH · DEFERRED_SCOPE. Một thẻ **bắt buộc** đang WAIT_INPUT/BLOCKED_TECH
**không** được đổi thành tuỳ chọn để bảng trông xanh.

| Mã | Việc | Ưu tiên | Phụ thuộc | Trạng thái | Bằng chứng / ghi chú |
|---|---|---|---|---|---|
| TB-G00 | Xác minh baseline và giữ bằng chứng gốc | P1 | — | **DONE** | [`g00-20260912-024738`](../review/technical/g00-20260912-024738/BAO-CAO.md). HEAD `780cf6d` khớp baseline review. **T01–T04 đều còn nguyên**, tái hiện lại chứ không chép. Probe theo Git: `scripts/ky-thuat/probe-gui-t01-t02.ts` — baseline **2/5 ca nói đúng**. T03 đo bằng hash trước/sau `npm run check`: **đổi**. Phạm vi: offline; chưa chạy browser/live/build. |
| TB-G01 | Đưa roadmap mới vào sổ thực thi | P1 | G00 | **DONE** | Chính mục này. Hai bảng tách riêng, một nguồn trạng thái. Bốn ô cần người thật → DEFERRED_SCOPE (B03, H01–H03), **không** xoá dữ liệu cũ. Sửa HEAD lạc hậu và câu "không còn việc nào Claude làm một mình được" (T04-b). |
| TB-G02 | Chốt cách kể sản phẩm theo rubric Technical | P2 | G01 | **DONE** | [ADR-0001](../adr/0001-doi-huong-technical-build.md) — rubric **30/25/25/20** chép nguyên văn từ thể lệ (dòng 141–148), bằng chứng theo từng mục, **mọi số qua đường đồng bộ**. **Phân biệt hai trạng thái track:** hướng phát triển (đội, đã quyết) ≠ đăng ký BTC (form 24/08, **chưa xác nhận cập nhật**) — ghi ở README, CLAUDE.md, `docs/nop-bai/README.md`. **Nói trước rủi ro lớn nhất:** mục 25 % có chữ *smart contract* mà Custos không có; **KHÔNG** thêm on-chain program để lấp (quyết định số 5) — câu hỏi cho BTC ở TB-H01. Guard `adrTechnical.test.ts` 10 bài, **đã kiểm phủ định 4 hướng**; bài rubric lúc đầu dùng `||` nên **không đỏ được** (đổi 30 %→35 % vẫn xanh) — nay neo vào dòng bảng, đòi tên **và** trọng số cùng dòng. **Và chính ADR trôi số ngay trong phiên:** nó gõ tay 556 test, một giờ sau thực tế 566 — đã đưa hai mốc vào `dong-bo-so-tai-lieu.mjs` + `NEO_DONG_BO` thay vì sửa tay. |
| TB-C01 | Phân loại đúng kết quả xác nhận **[T01]** | P1 | G01 | **DONE** | Thêm pha `thatBaiXacNhan` + `docKetQuaXacNhan()` đọc **nội dung** phản hồi. Probe: T01-b và T01-c từ `thanhCong` → đúng. 6 bài mới trong `gui.test.ts`, **đột biến 5 bài đỏ** khi trả lại hành vi cũ. Ranh giới giữ `unknown` có chủ ý — kiểu chặt sẽ chặn chính phép kiểm runtime. UI có màn hình riêng: *“đã lên chuỗi · đã hỏng · phí vẫn bị trừ”*, không dùng lại câu “chưa gửi đi”; giữ thẻ kết quả, chỉ đọc lại số dư. |
| TB-C02 | Giữ chữ ký và xử lý mất phản hồi gửi **[T02]** | P1 | C01 | **DONE** | `chuKy?` lấy chữ ký từ transaction đã ký **trước** khi gửi; `App.tsx` truyền và test canh đường dây đó. Chưa đủ chữ ký ⇒ `null` ⇒ nhánh chưa sẵn sàng gửi, **không** bịa ID toàn số 0. Chữ ký RPC lệch cục bộ ⇒ giữ cục bộ + ghi nhật ký. `maBase58` tự viết (bs58 là phụ thuộc **ngầm** của web3.js, không khai ở repo) — đối chiếu `PublicKey` và `bs58` trên 200 mẫu ngẫu nhiên, 0 lệch. **Phạm vi:** người gọi không truyền `chuKy` thì hành vi như cũ; probe giữ ca `T02-b` để nói đúng điều đó. |
| TB-C03 | Chống race, kết quả cũ và gửi lặp do giao diện | P1 | C02 | **DONE** | Ba khoá `ref` (`luotRef`, `dangGuiRef`, `dangKiemRef`) thay cổng đọc state React — `setState` không cập nhật closure của lượt render hiện tại nên `if (dangGui) return` để lọt. `ketQua` và `txCho` chỉ ghi cùng nhau sau `conDung()`; lượt cũ về muộn bị bỏ. **Ba tầng bằng chứng:** cơ chế ([`probe-race-c03.log`](../review/technical/g00-20260912-024738/probe-race-c03.log), 4/4 đúng như mong đợi) · đường dây (`c03Race.test.ts`, 7 bài đọc mã) · hành vi thật ([`soi-race-gui.log`](../review/technical/g00-20260912-024738/soi-race-gui.log), 5/5 trên Chromium). **Đã kiểm phủ định:** gỡ khoá ⇒ probe báo 2 lượt và FAIL; khôi phục ⇒ 1 lượt, PASS. **Ranh giới:** khoá trong MỘT tab; không tuyên bố exactly-once trên toàn mạng. |
| TB-C04 | Test không tự chạy eval hoặc ghi bằng chứng **[T03]** | P1 | G01 | **DONE** | Tách phần thuần sang `scripts/eval-ai-so.ts` (không `import.meta` — module thuần thì side effect không có đường vào). Nghiệm thu: hash artifact **không đổi** quanh `npm run check`, exit 0, git sạch — [`c04-hash-quanh-check.json`](../review/technical/g00-20260912-024738/c04-hash-quanh-check.json). CLI `npm run eval-ai` vẫn chạy và **giữ nguyên** `liveGanNhat` (38 mẫu, 28.878 token). Guard `c04KhongGhi.test.ts`, **đã đột biến 3 lần**: bài quét lệnh cấp module lúc đầu KHÔNG bắt được `console.log(...)` vì regex không nhận dấu chấm. |
| TB-C05 | Ngân sách RPC, hủy chờ và lỗi có cấu trúc | P1 | S01 | **DONE** | [`docs/NGAN-SACH-RPC.md`](../NGAN-SACH-RPC.md) — bảng 7 chặng (3 chặng làm giàu **đã có** đường lui riêng, 2 chặng bắt buộc cố ý không có). Kiểm trước khi thêm như thẻ yêu cầu: `coHan`/`coHanChung`/`moHan` đã đủ, **7 bài** sẵn có. **Sửa một rò rỉ đo được:** `boiThoiHan` không `clearTimeout` ⇒ 10 lượt để lại **đúng 10** handle `Timeout` (đo bằng `process.getActiveResourcesInfo()`, không suy từ thời gian chạy) — [`probe-timer-truoc-sua.log`](../review/technical/g00-20260912-024738/probe-timer-truoc-sua.log) 1/3 → [`sau-sua`](../review/technical/g00-20260912-024738/probe-timer-sau-sua.log) 3/3. Guard `boiThoiHanC05.test.ts` 5 bài, **đã kiểm phủ định**. **Khai rõ phần không làm được:** ngừng chờ ≠ huỷ request; `AbortSignal` có trong web3.js nhưng chỉ ở confirmation strategy và `sendAndConfirmTransaction` — không ở chặng nào Custos gọi (đã tra `index.d.ts`, và bản đầu tài liệu nói sai chỗ này). Lõi có **0** retry và **0** subscription. |
| TB-C06 | Ràng buộc kết quả với transaction và trạng thái quan sát | P1 | S01 | **DONE** | `packages/core/src/neo.ts` — `neoKetQua`/`khopNeo`/`quaCu`. **Lỗ hổng KHÁC với C03:** C03 lo hai lượt chồng nhau (đóng bằng `luotRef`); C06 lo **một** lượt, ID khớp, nhưng giao dịch bị đổi sau khi kiểm — `InspectResult` trước đây **không có trường nào nhận diện giao dịch đã kiểm**. Neo gồm băm message + người **được bảo vệ** + cluster; đổi blockhash **cũng** là đổi message ⇒ mặc định re-inspect. KHÔNG dùng chữ ký (giao dịch chưa ký thì chữ ký toàn 0). Ví chặn ký ở ba cổng: không có neo · neo lệch · kết quả quá cũ (30 s, là **quyết định được ghi**, không phải bảo đảm). [`probe-neo-c06.log`](../review/technical/g00-20260912-024738/probe-neo-c06.log) 7/7 · `neo.test.ts` 15 bài · **đã kiểm phủ định** 2 hướng. **Hai lỗi gặp và sửa:** `node:crypto` làm `vite build` đỏ (core phải chạy trên trình duyệt) ⇒ SHA-256 tự viết, đối chiếu `node:crypto` 300 mẫu + vector NIST + mốc khối 64 byte; và guard C03 đỏ vì mẫu regex — nới theo **số câu lệnh** thay vì số ký tự, vẫn đỏ khi chèn `await`. |
| TB-S01 | Threat model và ranh giới cưỡng chế | P1 | G01 | **DONE** | [`docs/bao-mat/THREAT-MODEL.md`](../bao-mat/THREAT-MODEL.md) — **bổ sung**, không thay `SECURITY-AUDIT.md` (audit là danh sách F1–F6 đã đóng; trang mới trả lời *ai nói dối được với Custos, qua đường nào*). 8 rủi ro, mỗi cái kèm cơ chế **đã đọc trong code** hoặc khai là giới hạn. Phát hiện khi đọc: **IDL giả không phải đường tấn công runtime** — bảng mã lệnh đóng băng lúc build, lúc chạy không có lượt tải IDL nào; rủi ro chuyển sang chuỗi cung ứng thời điểm build. Mục 4 phân biệt **decode · xác minh · đo được · kết luận**; mục 6 liệt kê **6 điều Custos không kiểm soát**. Guard `threatModel.test.ts` 10 bài — bài mạnh nhất gọi `kyHieuAnToan()` thật, không đọc chữ; **đã kiểm phủ định** cả hai hướng (bỏ bộ lọc ⇒ đỏ · xoá một giới hạn ⇒ đỏ). |
| TB-S02 | Đầu vào sai, giới hạn tài nguyên và dữ liệu lớn | P1 | S01 | **DONE** | **Kiểm trước khi thêm:** phần lớn đã có — `parseTokenAccount`/`parseMint` có `try/catch` trả `null`, base64 chặn ở 4096, `kyHieuAnToan` dùng allowlist ASCII (Bidi/Unicode tự loại), `amount` là `bigint`. Thêm `fuzz-s02.ts` **có seed** (mulberry32): 4.031 lượt × 5 seed ≈ 20.000, 0 vi phạm — đo 4 bất biến (không ném · không `safe` từ rác · không mất precision u64 tới 2⁶⁴−1 · không cho ký tự nguy hiểm ra). Ca thu nhỏ ghi kèm seed nên tái lập được. **Đã kiểm phủ định:** tháo `try/catch` ⇒ bắt được `len=64 owner=TokenzQd`; tháo bộ lọc ký hiệu ⇒ bắt `<script>` và chuỗi 10.000 ký tự. **Sửa một lỗ hổng đo được:** lỗi RPC mang **nguyên trang HTML** vào nhật ký kỹ thuật hiển thị trên UI (`502 Bad Gateway: <!DOCTYPE html>…`), và `VITE_RPC` có thể chứa credential ⇒ `locNhatKy.ts` lọc **tại chỗ ghi**; 6 bài, và bản đầu bỏ sót `<!DOCTYPE` vì regex `<\/?[a-z]` không khớp `<!`. Probe trình duyệt `soi-dau-vao-sai.py` **35/35** trên 7 ca — **đã kiểm phủ định**: bỏ giới hạn 4096 thì payload 12.000 ký tự sinh ra phán quyết *Cần xem kỹ* từ dữ liệu rác. |
| TB-S03 | Xử lý dependency theo đường thực thi thật | P2 | S01 | **DONE** | **11 → 5 advisory.** Hồ sơ 08/09 viết *“chờ bản vá không phải kế hoạch”* cho **cả 11** — đo lại thì câu đó **đúng với 5 high, sai với 6 moderate**: `uuid <11.1.1` và `stream-json <=3.4.0` có **giới hạn trên** ⇒ bản vá đã tồn tại, trong khi nhóm high có dải `*` ⇒ mọi phiên bản đều dính. Dấu hiệu nằm ngay trong `npm audit --json`; bản trước kết luận từ `fixAvailable` (đề xuất `web3.js@0.0.3`, hạ cấp 10 năm tuổi) nên bỏ qua luôn thông tin hữu ích cạnh đó. Vá bằng `overrides` **dải mở** — bản đầu dùng `^11.1.1` và **ép `rpc-websockets` xuống** từ `uuid@14.0.2` vốn đã sạch, `npm ls` báo `invalid`; dải `>=11.1.1` chỉ nâng nhánh cũ. **Bốn cổng xanh sau khi nâng:** 573 test · build hai app · devnet 6/6 · `thu-goi` 10/10 bẫy + 3/3 đối chứng. Phân loại runtime đo lại trên bundle hôm nay: `TokenAccountNotFoundError` **có**, `node:stream`/`stream-json`/`image-size` **không**. **5 high còn lại CHƯA vá** (dải `*`), giữ lập luận phơi nhiễm + 4 test canh ở `PHU-THUOC.md` 3.4. Guard `overridesPhuThuoc.test.ts` 7 bài, **đã kiểm phủ định 2 hướng** (tháo override · đổi `>=` thành `^`). |
| TB-B01 | Manifest, oracle và phân tầng bằng chứng | P2 | C04 | **DONE** | [`data/benchmark/manifest.json`](../../data/benchmark/manifest.json) v1, sinh bởi `npm run manifest-benchmark`. **Ba tầng, và chúng không thay thế nhau:** `l2-facts` **38/38** (luật không hồi quy) · `l1-replay` **29/38** (L1 bóc tách đúng) · `devnet-live` **19/38** đủ điều kiện (TB-B07). Chín mẫu thiếu tầng L1 vì **không có file giao dịch** — chúng là ca đối chứng dựng bằng cách sửa Facts trực tiếp; mười mẫu `real-mainnet` không chạy lại được trên Devnet. Cả hai con số đó là **sự thật cần ghi**, không phải chỗ cần lấp. **Oracle độc lập:** script **không import `packages/core`**, không gọi `danhGia`/`inspect`/`extractFacts`; kỳ vọng chép nguyên từ `index.json` nơi người gán nhãn viết tay (xác minh trong `thu-dataset.ts`). Hash dùng `git hash-object` — SHA-256 thô báo động giả mỗi lần checkout trên máy khác vì CRLF/LF. Mỗi mẫu mang **giới hạn của chính nó**; `held-out` = **0** (thư mục giữ lại cố ý trống). Guard `manifestBenchmark.test.ts` 11 bài, **đã kiểm phủ định 5 hướng** — thiếu trường ⇒ `boQua` · facts mất ⇒ `boQua` · sửa nội dung ⇒ hash đổi · sửa `index.json` quên sinh lại ⇒ `bamNguon` đỏ · gán `held-out` hoặc `devnet-live` sai ⇒ đỏ. |
| TB-B02 | Replay RPC chạy qua đường L1 sản xuất | P2 | B01 | **DONE** | `npm run replay-rpc` — **19/29 đạt · 0 hỏng · 10 chưa có fixture**, chạy qua `extractFacts` **sản xuất**, không qua bản sao. **Ba nghiệm thu, đo được cả ba:** (1) *offline* — runner không có `new Connection`/`fetch`/`node:http`, tách hẳn khỏi `capture-rpc.ts` (cổng `CUSTOS_CAPTURE=1`); (2) *nhạy fixture* — đổi một dữ kiện ⇒ Facts đổi, 19/19, qua 4 đường (`lamports` · `account.data` · `sim.err` · `__loi`); (3) *thiếu fixture báo rõ* — **19/19 tất định** bit-for-bit. **KHÔNG so với Facts đóng băng 21/08:** bản đầu ra 18/19 lệch và không cái nào là lỗi replay (decoder tốt lên, ví devnet được airdrop, bản cũ thiếu 5 trường schema). Bản đóng băng là **ảnh chụp L1 ngày 21/08, không phải ground truth**; độ lệch nay **báo cáo**, không làm đỏ. **10 mẫu chưa chạy là từ chối CỐ Ý:** chúng là `real-mainnet`, và capture không cho ghi fixture mainnet bằng endpoint devnet — ALT/account không tồn tại ở đó nên fixture sẽ ghi *một sự thật của devnet* dán nhãn mainnet. Chờ endpoint mainnet, không chờ code. Guard `replayRpc.test.ts` **13 bài, mutation 5 hướng đều đỏ đúng chỗ** — bỏ đọc hộp `thieuFixture()` ⇒ đỏ · bỏ chặn mainnet-trên-devnet ⇒ đỏ · thêm fixture thừa ⇒ đỏ · nói quá `19/29`→`29/29` ⇒ đỏ · gộp hai cột bảng ⇒ đỏ (bài này **ban đầu KHÔNG đỏ được**, đã sửa để neo vào hàng tiêu đề thay vì quét cả trang). **Giới hạn:** fixture **thừa** chưa bị phát hiện — `daDung()` có nhưng runner chưa đối chiếu. |
| TB-B03 | Ma trận hành vi Solana có ca đối chứng | P2 | B02 | **DONE** | [`docs/bao-mat/MA-TRAN-HANH-VI.md`](../bao-mat/MA-TRAN-HANH-VI.md) — **19 họ ca** của mục 16, mỗi ô ghi ca dương · đối chứng · file bằng chứng · tầng. **Rà trước khi viết, và kết quả rà đổi hẳn việc phải làm:** 14/14 luật ĐÃ có cả ca dương lẫn ca đối chứng từ trước — B03 không phải việc lấp lỗ hổng đối chứng mà là việc **kiểm chất lượng và ghi đúng phạm vi**. Ba ô thật sự thiếu: **(1) Legacy** — cả repo không một dòng nhắc `legacy`; đo trước khi viết thì `VersionedTransaction.deserialize` NHẬN byte legacy (`version:"legacy"`, `addressTableLookups: []`) và `extractFacts` chạy trọn (coverage 1/1). Nên `legacyTx.test.ts` (3 bài) **khoá hành vi đang đúng**, không phải bản vá — kèm một bài canh legacy KHÔNG tốn lượt RPC hỏi ALT. **(2) Cancel** — lỗi THẬT, chưa ai biết: `onHuy` dọn màn hình nhưng không đụng `luotRef`, nên lượt `inspect()` đang bay vẫn thoả `conDung()` và khi về thì dựng lại **cả ba** thứ vừa dọn — đo ở `probe-race-c03.ts` ca **C03-e**: `ketQua=danger txCho=giao-dich-A neo=luot-1`. `txCho` và `neo` là đúng hai thứ `kyVaGui` đòi, nên người dùng bấm *"Chặn & huỷ"* một giao dịch Đỏ rồi **nút Ký sống lại trên chính nó**. Sửa: `luotRef.current++` + `neoRef.current = null`; ca **C03-f** đo cách đúng, `soHongNgoaiDuKien: 0`. **(3) Token-2022 — khoảng cách lớn nhất:** đọc được **2/26** extension (`PermanentDelegate`, `TransferHook`). 23 cái còn lại KHÔNG bị từ chối an toàn — `parseMint` trả `MintFact` hợp lệ trông y hệt mint trơn, `Facts` không có trường nào nói *"mint này còn extension tôi không đọc"*, và `computeCoverage` chỉ đếm **instruction**. Nên mint có `TransferFeeConfig`/`PausableConfig` đi qua với coverage đầy đủ và verdict không biết gì. **Ghi thay vì sửa**: vá đúng cách cần thêm trường Facts + luật L2 thứ 15, cả hai ngoài phạm vi B03 và đụng giao kèo kiểu đã đóng băng. **12/14 ca dương có raw tx + fixture replay**; `R13-pos`/`R14-pos` chỉ có Facts dựng tay — cả hai **dựng được**, ghi là việc còn lại chứ không phải giới hạn kỹ thuật. Guard `maTranHanhVi.test.ts` **8 bài, mutation 6 hướng đều đỏ đúng chỗ** — nói quá 2→5 extension · xoá số 23 · viết "hỗ trợ Token-2022" · xoá câu `devnet-live` còn 0 · nói quá 12/14→14/14 · nêu file bằng chứng không tồn tại. (M4/M6 lần đầu **vô hiệu vì bash nuốt backtick** — mutation chưa chạm file; chạy lại bằng script tự kiểm "file trên đĩa có đổi không" thì cả hai đỏ.) |
| TB-B04 | Kiểm thuộc tính và đối kháng có khả năng phát hiện hồi quy | P2 | B03 | **DONE** | [`docs/bao-mat/MUTATION-B04.md`](../bao-mat/MUTATION-B04.md) + biên bản máy đọc [`data/benchmark/mutation-b04.json`](../../data/benchmark/mutation-b04.json). **Nghiệm thu thẻ KHÔNG phải viết thêm test** — nó là *"mutation ở từng ranh giới trọng yếu làm bài kiểm liên quan đỏ"*, và cấm rõ *"đặt quota số test để lấy điểm"*. **7 ranh giới, mỗi cái sửa MỘT dòng mã sản phẩm rồi hoàn nguyên:** FS1 mô phỏng hỏng **2** bài đỏ · FS2 chưa đọc hiểu mà chạm tài sản **1** · FS3 ALT **0 → 1** · FS4 account không đo được **3** · `caoHon()` **56** · `level` chỉ do L2 **3** · `expectedAction` bất đối xứng **1 → 2**. **5/7 ngay từ đầu; hai ô còn lại là phần đáng giá:** **FS3** — tắt hẳn fail-safe ALT mà *không một bài nào trong 609 đỏ*. Không phải vì nó thừa: luật 10 bắt cùng điều kiện và nâng verdict trước, nên bài cũ xanh dù lớp nào làm việc cũng được. Chứng minh FS3 có thật bằng `danhGia(facts, LUAT.filter(l => l.id !== 10))` ⇒ `warning` với `reasonCodes: []` — rỗng nghĩa là không luật nào phát mã, `warning` chỉ có thể từ fail-safe. **EXP** — đảo `!==`→`===` (lật ngược hoàn toàn quy tắc bất đối xứng) chỉ đỏ **1** bài; bài *"KHỚP ⇒ không tắt cảnh báo"* vẫn xanh vì nó đặt sẵn `aiAdvisory` từ L3. Thứ phân biệt được là `loiKhaiLech` — trường CHỈ đặt khi khai lệch; khai khớp mà có nó nghĩa là đang tố một dApp trung thực. **Năm phép biến đổi của thẻ đều ĐÃ có bài từ trước** (symbol · khai khớp · AI hỏng · mất account · nhiều signer) — việc của thẻ là kiểm lại, và grep theo từ khoá đã báo sai *"0 file"* cho cả năm, phải đọc tên bài mới thấy. **Fuzz vào bộ hồi quy:** `npm run doi-khang` (~3,0 s) chạy fuzz-s02 + 4 probe, trước đó không có lệnh nào. Seed có tác dụng thật — đo bằng dấu vết rnd: seed 1 → 795.574 số bắt đầu `8,0,135…`, seed 42 → 794.020 số bắt đầu `8,114,218…`. Guard `doiKhangB04.test.ts` **8 bài, mutation 5 hướng đều đỏ** — để sót `if (false)` · xoá ranh giới · xoá bài FS3 · sửa biên bản thành "7/7 ngay từ đầu" · gỡ fuzz khỏi lệnh gộp. **Không mutation nào còn trong mã sản phẩm**, xác minh bằng so sánh byte. |
| TB-B05 | Tập đánh giá mới và công bố giới hạn thống kê | P2 | B03 | **DONE** | [`docs/BENCHMARK.md` mục 8](../BENCHMARK.md) · `npm run danh-gia-b05` (offline) · biên bản [`data/benchmark/danh-gia-b05.json`](../../data/benchmark/danh-gia-b05.json). **Ràng buộc khó nhất của thẻ là nguồn kỳ vọng phải ĐỘC LẬP với verdict — và đặc tả nội bộ KHÔNG đạt:** `DAC-TA-CORE.md` commit `63959f3` lúc 21:55:31, engine L2 commit `42c33d0` lúc 21:57:03 cùng ngày 21/08 — **92 giây**, cùng người, cùng phiên. Dùng nó rồi gọi là độc lập sẽ lặp đúng lỗi vòng tròn mà `BENCHMARK.md` mục 1 đã tự cảnh báo. Nguồn dùng thay thế: **tài liệu RPC chính thức của Solana** (`simulateTransaction`), tồn tại trước dự án. **Ba tính chất, và chỉ HAI được chứng minh:** P1 *mô phỏng hỏng ⇒ không có trạng thái sau* — áp dụng 4/19, đạt 4/4, mutation gỡ vế `!v.err` ⇒ **tụt còn 1/4**; P2 *không đo được ⇒ phải ghi vào `accountKhongDoDuoc`* — áp dụng 4/19, đạt 4/4, mutation tắt nhánh ghi khuyết ⇒ **tụt còn 0/4**; **P3** *ALT không giải được ⇒ không bao giờ `safe`* — áp dụng 1/19, đạt 1/1 nhưng **CHƯA CHỨNG MINH bắt được lỗi**. Mẫu duy nhất kích hoạt P3 (`R10-pos`) cũng có `simulationOk: false`, nên fail-safe 1 nâng verdict trước khi lớp ALT kịp làm gì — tắt fail-safe 3 hay tắt luật 10 đều không kéo nó xuống. **Cùng hình dạng với phát hiện FS3 ở B04**: hai lớp chồng nhau, không tách được bằng dữ liệu hiện có; cần một mẫu *ALT hỏng mà mô phỏng THÀNH CÔNG*, bộ 19 fixture không có. Ghi ô trống thay vì bỏ P3 đi — bỏ đi thì bảng thành 3/3 hoàn hảo và mất luôn thông tin. **19/38 mẫu KHÔNG đánh giá được, nhóm riêng, không trộn mẫu số:** 10 mẫu `real-mainnet` (capture từ chối ghi fixture bằng endpoint devnet) + 9 mẫu không có file giao dịch. 19 + 19 = 38. **Không phân loại TP/FP/TN/FN** — thẻ cho phép *"chỉ khi bài toán nhị phân có nghĩa"*, và ở đây một mẫu có thể vừa đạt tính chất này vừa sai tính chất kia. **Không đổi tên 38 mẫu cũ thành held-out**: `data/seed/giu-lai/` vẫn trống, manifest vẫn `held-out: 0`. Guard `danhGiaB05.test.ts` **9 bài, mutation 5 hướng đều đỏ** — khai P3 đã chứng minh · tài liệu xoá dấu **CHƯA** · xoá ranh giới accuracy · xoá cụm *independent security validation* · đổi nguồn tính chất sang đặc tả nội bộ. |
| TB-B06 | Đo đóng góp riêng của Custos bằng so sánh thành phần | P2 | B03 | **DONE** | [`docs/bao-mat/SO-BASELINE.md`](../bao-mat/SO-BASELINE.md) · `npm run so-baseline` (offline). **Ba baseline định nghĩa TRƯỚC, và cố ý KHÔNG làm yếu** — thẻ cấm *"làm hỏng baseline để tạo thắng lợi"*: B1 được cho **cả decoder của Custos** (`decoded.kind`), giới hạn duy nhất là không nhìn inner; B2 được cho **cả SOL lẫn token**, có trừ phí. **Kết quả 5 ca: Custos lệch kỳ vọng người gán 0/5 · thêm phát hiện 1 ca · baseline báo nhầm 2 ca.** Một ca thắng trên năm nghe ít, và con số đó đúng hơn một con số cao. **B06-1 `R01-pos` là ca nguyên tắc kiến trúc kiếm được điểm:** `setAuthority` đổi chủ token sang ví lạ, `amount` giữ nguyên 500000000n, `solDelta` chỉ có phí −5000. B1 **đọc hiểu được** lệnh (in ra đúng chữ `setAuthority`) nhưng im vì không có LUẬT nào; B2 mù vì không một đơn vị nào rời ví. Đọc hiểu ≠ biết hậu quả. **B06-4/B06-5 là hai ca baseline BÁO NHẦM**, ghi vào vì thẻ đòi lưu cả ca bất lợi: `R13-neg` là lệnh mua — 2 SOL ra, **7,45 tỉ đơn vị token vào** — B2 chỉ cộng vế ra nên kêu oan, luật 13 kiểm `coNhanLai` nên im. **B06-3 ghi bằng HAI con số:** Facts 21/08 có `accounts: 2` với `lamportsAfter: 0` ⇒ phát `SOL_ROI_VI`, một cáo buộc dựng trên trạng thái sau của mô phỏng ĐÃ HỎNG; L1 hôm nay trả `accounts: 0`, `solDelta` rỗng, verdict `MO_PHONG_HONG + TRANG_THAI_DO_KHUYET` — cổng `coDuLieuAccount` đã chặn. Ghi mỗi bản cũ là tố sản phẩm một lỗi đã vá; ghi mỗi bản mới là giấu một ca baseline thắng Custos bản cũ. **Một lỗi đã sửa trong chính bảng này:** bản đầu suy nhãn đúng/sai từ `level` của Custos — oracle vòng tròn. Nay đọc `kyVong` từ `index.json`. Và `khong-phai-danger` (17/38 mẫu, cả 10 mẫu MN) là **nhãn khoảng**, so bằng `!== danger` theo đúng quy ước `dataset.test.ts`, không so bằng. Guard `soBaselineB06.test.ts` **10 bài, mutation 5 hướng đều đỏ** — bịt mắt B1 · làm yếu B2 · xoá đọc `index.json` · so bằng nhãn khoảng · xoá câu cảnh báo Phantom. **KHÔNG phải so với Phantom/Blockaid** — ba baseline là ba cách triển khai mô tả trong chính script. |
| TB-B07 | Kiểm chứng live với phạm vi tối thiểu, an toàn | P2 | B03 | DONE | Devnet live 13/13; 3 tx chưa ký + 1 fault injection. Artifact codex-20260916/b07.json và raw RPC; không broadcast. |
| TB-X01 | Trace cảnh báo tới bằng chứng sản xuất | P2 | B06 | **DONE** | [ADR-0002](../adr/0002-chan-doan-tuy-chon.md) · `RuleHit.bangChung` + `InspectResult.chanDoan` (cả hai TUỲ CHỌN). **Bước 1 của thẻ là RÀ, và việc rà tìm ra hai lỗi thật.** `diff.ts` nối cảnh báo với dòng bảng bằng `hits.some(h => h.detail.includes(địa_chỉ))` — dò base58 bên trong một câu tiếng Việt. Hỏng khi luật nhúng định danh KHÁC thứ bảng đang dò: **(1) luật 13** nói về SOL người dùng, không nhắc địa chỉ nào ⇒ dòng wSOL không bao giờ đỏ (đã vá trước bằng ngoại lệ `h.ruleId === 13`); **(2) luật 11** cộng theo `mint` nên `detail` nhúng mint, bảng dò **địa chỉ token account** ⇒ trên `R11-pos` hai token rời ví sạch (500000000→0 và 300000000→0), verdict Vàng với `OUTFLOW_KHONG_KHOP`, mà **cả hai dòng số dư vẫn `info`** — 0 dòng đỏ. Lỗi này chưa ai vá. **Bản sửa chung, không vá theo từng luật:** `RuleHit.bangChung?: BangChung[]` mang ID ổn định (`{loai, khoa}`, khoá base58); `diff.ts` nối theo `loai+khoa`. Ngoại lệ `h.ruleId === 13` **đã gỡ** — `h.ruleId` trong mã thực thi của `diff.ts` nay là **0**. Đo: `R11-pos` **0 → 2 dòng đỏ**; tổng dòng `danger` cả bộ giữ nguyên **10** (chuyển luật 13 sang cơ chế chung không đổi hành vi). **Chẩn đoán tuỳ chọn:** `chanDoan: true` ⇒ `InspectResult.chanDoan` có `phienBan: 1`, danh sách cảnh báo (`ruleId` → `reasonCode` → `bangChung`), `thieuBangChung` **đếm thật** (12/14 luật chưa khai), và `nguon.tang: L1`. **Nghiệm thu đo bằng số, đếm TỪNG method:** bật/tắt cho verdict giống hệt, `diff` giống hệt, **RPC = 6 cả hai chiều** (`getFeeForMessage`1 · `getMultipleAccountsInfo`3 · `simulateTransaction`1 · `getSignaturesForAddress`1). **Impact bundle:** 352.482 → **353.254 byte (+772, +0,22 %)**, phần lớn là dòng hiển thị trong `mucKyThuat.ts`; `ChanDoan` là type-only (0 byte runtime). Consumer không bật vẫn trả giá đó — ghi vào ADR chứ không giấu. Guard `chanDoanX01.test.ts` **9 bài, mutation 5 hướng đều đỏ** (bỏ cờ bật · làm tròn `thieuBangChung` về 0 · nhét text tiếng Việt vào `khoa` · trace mang mã lạ · xoá `phienBan`) + `adrChanDoan.test.ts` **7 bài** + 2 bài mới trong `bang-nhat-quan.test.ts`, mutation 4 hướng đều đỏ. |
| TB-X02 | Chế độ xem kỹ thuật và ca đối chứng trong demo | P2 | X01 | **DONE** | **Rà trước, và phần lớn nghiệm thu ĐÃ đạt từ trước:** progressive disclosure đã có (`aria-expanded`, khối "Chi tiết kỹ thuật" đóng sẵn), hai kịch bản `tanCong`/`lanhTinh` chuyển được, nhãn mock đã có, nút Huỷ trên mobile xếp TRƯỚC nút Ký (`grid` dọc mặc định, `sm:order-1` chỉ đảo khi lên desktop). **Chuyển kịch bản KHÔNG giữ trace cũ** — đo bằng mô hình cơ chế `luotRef` + dọn state: lượt chậm về sau bị `conDung()` loại, `ketQua`/`txCho`/`neo` đều thuộc kịch bản mới. **Hai chỗ thiếu thật, đã vá:** **(1) trace của X01 không ai xem được** — SDK mặc định TẮT `chanDoan` (ADR-0002), demo không bật thì cả thẻ X01 nằm chết trong SDK. Nay demo bật `chanDoan: true` ở lượt kiểm chính; đây là ví DEMO cho giám khảo, mặc định của SDK vẫn tắt. Không thêm lượt RPC nào. **(2) cluster là NHÃN CỨNG, không phải thứ đo được** — chữ "Devnet" nằm rải rác trong câu văn ở bốn chỗ, nên một endpoint localnet vẫn hiện *"Đang mô phỏng trên Devnet"*. Nay có `clusterCua()` suy từ endpoint ĐANG dùng (`chonRpc` ưu tiên `ht.rpc` rồi mới `VITE_RPC`, nên cluster có thể khác thứ người đọc đoán) và `hostCua()` lọc bỏ path/query vì credential RPC thương mại nằm ở đó. **Endpoint riêng (Helius/QuickNode) trả `không rõ`, KHÔNG đoán bừa `mainnet-beta`** — gắn nhãn đắt nhất cho một thứ chưa biết là cách tệ nhất để sai. Khối bối cảnh nằm TRONG phần đóng sẵn (thẻ đòi *"giữ luồng mặc định gọn"*), gồm: nguồn kết quả (live vs mock, nói thẳng *"KHÔNG phải kết quả thật"*), cluster, endpoint, phần chưa đọc hiểu. Guard `boiCanhX02.test.ts` **10 bài, mutation 7 hướng đều đỏ** — đoán bừa endpoint lạ thành mainnet · đảo thứ tự mainnet lên trước devnet · trả cả URL thay vì host · hardcode cluster · tắt `chanDoan` · nhãn mock không nói rõ. (Một mutation lần đầu không đỏ vì **chính nó sai**: hoán vị devnet↔testnet, hai nhánh loại trừ nhau nên không đổi hành vi.) **Probe trình duyệt ĐÃ CHẠY, xanh:** `scripts/kiem-trinh-duyet/soi-boi-canh-x02.py` trên Chromium 149.0.7827.55 — khối bối cảnh dựng được, mock nói rõ *"KHÔNG phải kết quả thật"*, ca đối chứng không hiện Nguy hiểm, nút huỷ mobile 43,8 px. **Mutation 3 hướng đều đỏ** (xoá nhãn mock · xoá dòng Cluster · tắt cả khối bối cảnh). **Probe bắt được ba lỗi — cả ba là của CHÍNH NÓ, không phải của sản phẩm**, và đó là giá trị của việc chạy thật thay vì tin bài đọc mã: (a) mock chỉ nạp trong `chayKiem()`, tức chỉ khi người dùng BẤM kịch bản — mở URL rồi chờ thẻ kết quả là chờ một thứ không bao giờ tới; thiếu bước bấm nên probe báo đỏ nhầm sản phẩm. (b) đòi dòng "Dấu vết" ở luồng mock là đòi sai chỗ: `data/mocks/*.json` là `InspectResult` đóng băng từ TRƯỚC X01, không có `chanDoan` — và **không nên có**, vì bắt mock mang dữ liệu của lượt chạy thật đúng là "live giả" mà thẻ cấm. (c) gọi nút huỷ 43,8 px là "vi phạm" là **nói sai về tiêu chuẩn**: WCAG 2.2 (2.5.8) đặt ngưỡng **24 px**, còn 44 px là khuyến nghị cho ngón tay — `soi-vung-bam.py` đã chốt quy ước đó từ trước và probe mới phải dùng lại, không phát minh ngưỡng thứ hai. |
| TB-I01 | Gói phát hành và ví dụ dùng SDK không phụ thuộc monorepo | P2 | C04 | **DONE** | `npm run thu-goi` — **6/6 bước xanh trên mã hiện tại**. **Rà trước: 8/8 nghiệm thu đã có sẵn trong `thu-goi-nguoi-ngoai.mjs`**, nên việc thật của thẻ là CHẠY LẠI để có bằng chứng phủ mã hiện tại, không phải viết thêm. Lượt gần nhất đo 12/09 với dấu vết `3a969d1b`/103 file, trong khi phiên này đã sửa `packages/core/src` và `packages/types/src` — bằng chứng cũ **không phủ** mã mới. **Kết quả lượt chạy 14/09:** 14 luật · ca dương `warning NGUOI_DUNG_KHONG_RO` · ca đối chứng `safe` · `inspect()` chạy được từ JS thuần bằng `node` TRẦN (không cờ bóc kiểu) · `tsc --noEmit` OK với **`strict: true` và `skipLibCheck: false`** · **10/10 bẫy đối kháng bị CHẶN**, `lotChu=0 doiLevel=0` · 3 ca đối chứng đi lọt đúng (câu hợp lệ, L3 ném lỗi, L3 treo 301 ms — `level` giữ nguyên cả ba). **Subpath `./anthropic` có kiểm** (dòng 97 của script), và `@anthropic-ai/sdk` **không** bị npm kéo về khi bên tích hợp không yêu cầu — peer dependency optional hoạt động thật. **Consumer không dùng alias workspace**: cài từ `file:` tarball, project nằm ngoài monorepo. **Lệnh README chạy thật:** `node scripts/dong-goi-sdk.mjs <thư-mục>` sinh đúng ba tarball (`types-0.1.1`, `core-0.1.1`, `ai-0.2.0`). **Ghi đúng phạm vi:** đây là chứng minh khả năng tích hợp kỹ thuật, **không** phải pilot bên thứ ba — `data/tich-hop/ket-qua.json` ghi sẵn câu đó, và nó là artifact của `thu-tich-hop.mjs` (dApp mẫu trên Devnet), KHÁC script này. `thu-goi` cố ý không ghi artifact; nghiệm thu I01 không đòi. |
| TB-I02 | Consumer mô phỏng ví có hợp đồng ký rõ | P2 | I01 | **DONE** | `vi-du-tich-hop/src/ky.js` + `ky.d.ts` · guard `kyHopDongI02.test.ts` **11 bài**. **Lỗ thật của thẻ:** `tich-hop.js` dừng ở quyết định `cho`/`lyDo` — **không có ai ký**, nên nghiệm thu *"signer không được gọi ở các ca chặn/hủy"* không kiểm được: không có signer để đếm. **Hợp đồng ký đặt ở FILE RIÊNG, có lý do đo được:** `dongMaTichHop` đếm đúng `tich-hop.js` (`thu-tich-hop.mjs:315`) và được công bố ở **sáu chỗ**; nhét hợp đồng ký vào đó sẽ thổi con số lên trong khi công sức tích hợp Custos không tăng. Và thẻ nói rõ *"policy do consumer áp, SDK chỉ trả thông tin"* — `tich-hop.js` giữ nguyên **30 dòng**. **Năm chốt trong `kySauKhiKiem`:** CHẶN ⇒ không ký · HỎI mà chưa đồng ý ⇒ không ký (`nguoiDungDongY` mặc định `false`, fail-safe: quên truyền cờ thì không ký) · `khopNeo` trước khi ký (TB-C06 áp ở tầng consumer — dApp tráo giao dịch giữa lúc kiểm và lúc ký thì băm `message.serialize()` không khớp) · `quaCu` ⇒ không ký · chỉ tới đó signer mới được chạm vào. **`ky.d.ts` thay vì `ky.ts`:** consumer cố ý là JS thuần (TB-I01 đo `node` trần, không cờ bóc kiểu), nhưng `tsconfig.include` không phủ `vi-du-tich-hop` nên test TS nhận `TS7016`. Ba đường, hai sai: bật `allowJs` đổi phạm vi build để chiều một bài kiểm; `@ts-expect-error` giấu luôn mọi lỗi kiểu sau này. Declaration khớp implement: 8/8 tham số, 5/5 giá trị `lyDo`. **Mutation 7 hướng đều đỏ** — gỡ chốt CHẶN · gỡ chốt HỎI · gỡ `khopNeo` · gỡ `quaCu` · đổi mặc định `nguoiDungDongY` thành `true` · signer nhận bytes của tx đã kiểm · signer nhận object tx đã kiểm. **Hai mutation cuối lần đầu KHÔNG đỏ, và đó là lỗ thật của guard:** mọi ca được phép ký đều có `txSapKy` vắng mặt ⇒ `sapKy === tx` ⇒ hai bytes trùng nhau, phép so không phân biệt được. Bài mới dựng **hai object khác nhau mang cùng nội dung** — neo khớp (băm theo bytes) nên vẫn cho ký, và lúc đó bytes truyền cho signer mới là thứ duy nhất phân biệt. **Không chạm mạng, không ký thật:** `ky.js` chỉ băm bytes rồi gọi lại hàm do ví truyền vào; guard cấm `fetch`/`Connection`/`sendTransaction` trong file đó. |
| TB-X03 | Nghiệm thu UX, khả năng tiếp cận và trạng thái hiếm | P2 | X02, C01, C02, C05 | **DONE** | **10 probe chạy lại trên mã hiện tại**, tất cả PASS, dấu vết giao diện `c90436fd` — bằng chứng cũ đo trên `780cf6d` đã **lạc hậu** vì phiên này sửa `SoLieu.tsx`/`locNhatKy.ts`, nên phải chạy lại chứ không tin số cũ. **Phủ từng tiêu chí:** 4 khung 320/375/768/desktop (`soi-ket-qua-trong-tam-nhin` 3/3 · `soi-trinh-duyet` axe 40/40) · Tab/Shift-Tab/Enter/Space + vòng focus + thứ tự an toàn-trước (`soi-ban-phim-va-phong-to` 16/16) · reduced motion (0 animation còn chạy) · zoom 320px & 640px reflow AA, chữ dài gấp mười, 0px tràn ngang · payload sai 35/35 (`soi-dau-vao-sai`) · huỷ/lỗi có phản hồi thấy được (`soi-yeu-cau-va-huy` 4/4) · cấu hình hỏng 5/5 không trắng trang · race GUI 5/5 · vùng bấm **26/26 đạt 44px**, 0 vi phạm. **Ghi nhận không phải vi phạm:** chữ dùng px cố định (font-size gốc 16→32px nhưng chữ giữ 12,5px) — phóng to trình duyệt vẫn chạy, hai ca reflow chứng minh; cái mất là thiết lập cỡ chữ mặc định, **không** phải 1.4.4. Chuyển px→rem là việc riêng. **Lỗ bằng chứng tìm ra và đã vá:** 4 probe chỉ `print` rồi `sys.exit`, không để lại biên bản — *"một phép đo chỉ in ra màn hình thì lần sau lại phải tin lời kể"*, đúng câu `soi-trinh-duyet.py` tự viết. Thêm `ghi_bang_chung` cho cả bốn. **Và bản vá đó tự sinh ra một lỗi:** `soi-yeu-cau-va-huy.py` ghi `soKiem: 0` kèm `dat: true` vì nó `print` trực tiếp ở hai khối thay vì đi qua hàm gom — **một biên bản nói "đạt" với 0 phép kiểm tệ hơn không có biên bản**. Guard mới `bangChungA11y.test.ts` (4 bài, quét cả thư mục nên probe thứ 11 tự được canh; **mutation 2 hướng đỏ**: xoá `dauVet`, đặt `soHong=2` mà `dat=true`). **Phạm vi chưa kiểm, giữ nguyên công bố:** WebKit · Firefox · thiết bị thật · mạng bị bóp. **Hai biên bản lạc hậu có chủ ý:** `ban-trinh-dien.json` (P03) và `phong-van-vong-2.json` thuộc roadmap CŨ, lạc hậu vì dấu vết giao diện đổi chứ không vì thứ chúng đo thay đổi — không chạy lại trong phạm vi thẻ này. |
| TB-I03 | CI tách kiểm tất định, browser và live | P2 | C04, B04, I02 | **DONE (phần local)** | **Chỉ phần cấu hình + kiểm tương đương local**, đúng quy tắc trạng thái thẻ định trước; phần **remote CI trên revision ứng viên** là đầu việc con của **TB-H02**, WAIT_INPUT — không gộp hai thứ vào một ô. **Rà trước khi sửa: 5/14 tiêu chí THIẾU** (replay, đối kháng, timeout, job browser, upload artifact khi lỗi); sau khi sửa **14/14 đạt**. **Ba tầng, ba loại lý do đỏ khác nhau, nên ba cổng khác nhau:** tất định (đỏ ⇒ sản phẩm sai, chặn deploy) · browser (đỏ có thể do Chromium tải hỏng) · live Devnet (đỏ có thể do RPC công cộng chậm). Hai tầng sau là `workflow_dispatch`, **không** nằm trên đường push — giữ nguyên quyết định `soi-trinh-duyet.py` đã tự khai từ trước (*"KHÔNG nằm trong CI, và cố ý như vậy"*), vì một CI bị bỏ qua tệ hơn không có CI. **Thêm vào job build:** `replay-rpc` (L1 bóc response RPC thô ra Facts — unit test bắt đầu SAU bước bóc nên không phủ câu này) và `doi-khang` (5 probe, từng bắt 4 lỗi sản phẩm thật). **Đo được là offline THẬT, không tin chú thích:** chặn `http`/`https`/`net`/`dns`/`fetch` rồi chạy lại ⇒ cả 5 exit 0. Ba script đầu im lặng ở lượt đo đầu vì chúng `process.exit()` trước khi tới dòng kết luận — phải bắt ở `process.on("exit")` mới đo được. **Chi phí:** +4,3 s trên job ~15,3 s (nay 20,5 s cục bộ). **`timeout-minutes` cho MỌI job** — mặc định GitHub là 6 TIẾNG, một bước treo chiếm runner cả buổi tối mà job vẫn hiện "đang chạy". **Không secret, không mainnet ở bất kỳ tầng nào:** endpoint đọc từ `hien-truong.json` → Devnet công cộng, `thu-tich-hop.mjs` không đọc một biến môi trường nào (grep `env.` rỗng). **`npx --yes wait-on` đã bị loại** — nó kéo bản mới nhất lúc chạy, tức một phụ thuộc không ghim lọt vào đúng workflow mà mọi thứ khác đã ghim; thay bằng vòng `curl` có sẵn trên runner. **Guard `ciBaTang.test.ts` 11 bài, mutation 8 hướng đều đỏ** (gỡ timeout · gỡ replay · gỡ đối kháng · bỏ `workflow_dispatch` · bỏ `if: always()` · đảo thứ tự sinh số liệu · nới ghim Node thành `"24"` · nhét lại `npx --yes`). **Không dựng parser YAML:** repo không có `pyyaml`/`js-yaml`, và cài thêm gói ngay trước hạn thi để chiều một phép kiểm là đổi lockfile lấy sự tiện — kiểm theo dòng đủ bắt thụt lề, khoá trùng và bất biến bị gỡ. **Ba bài đầu tiên đỏ vì lý do SAI, không phải lỗi workflow:** regex `/^  [a-z0-9-]+:$/` bắt trúng `  push:` trong khối `on:` (thụt đúng 2 dấu cách như một job), và `indexOf("npm run check")` bắt trúng chú thích giải thích thứ tự thay vì bước thật. Đã neo từ `jobs:` trở xuống và lọc dòng chú thích trước khi quét. |
| TB-P01 | Phép đo performance đầy đủ ngữ cảnh | P2 | C05, B03, X02 | **DONE** | `soi-do-tre.py` trên **bản production** (`vite preview`, base `/Custos-Solana/`), Chromium 149. **Rà trước: 9/11 yêu cầu đã có sẵn** (tách cold/warm, số RPC, timeout, byte JS truyền/giải nén, cache, tách latency AI, lưu từng lượt). Hai chỗ thiếu, đã vá: **(1) đo 5 lượt, thẻ đòi ≥30** — nâng lên 30; **(2) lượt hỏng BIẾN MẤT** — vòng đo `continue` im lặng khi timeout hoặc không thấy nút, nên một lượt 20/30 hỏng vẫn báo trung vị đẹp trên 10 lượt còn lại; tập đó lọc theo đúng tiêu chí *"lượt nào xong thì giữ"*. Nay ghi `luotHong` kèm lý do và `tyLeHoanTat`. **Suýt mất lần thứ hai:** `hong` đã thu thập đủ nhưng ba dòng ghi kết quả chỉ lấy `ms` — thu rồi vứt còn tệ hơn không thu, vì người đọc mã tưởng đã có. **Kết quả 30 lượt (14/09): 30/30 hoàn tất, 0 hỏng** · trung vị **1596 ms** · p95 quan sát **3874 ms** · dải **834–3877 ms**, dao động **4,6×** · RPC 7–11 lượt/lần kiểm. **Số công bố cũ ~850 ms SAI và đã sửa ở ba chỗ** (`HIEU-NANG.md`, `README.md`, `ADR-0001`): nó là trung vị của **4 lượt** sau khi bỏ lượt đầu, còn 1596 ms là trung vị của cả 30. Chênh gần gấp đôi, và số lớn hơn mới đúng. Ba chỗ đó **không có anchor đồng bộ**, nên chúng trôi im lặng — đúng loại lỗi `claim.test.ts` sinh ra để chặn. **Gọi đúng tên percentile:** trường là `p95QuanSat`, và `khongDo` khai rõ p95 của TỔNG THỂ vẫn chưa đo được — một phiên trên một máy không đỡ được câu *"95 % người dùng thấy dưới 3874 ms"*. Guard `hieuNangP01.test.ts` **7 bài, mutation 5 hướng đều đỏ** — bớt lượt hoàn tất · khai lượt hỏng mà mảng rỗng · hạ về 5 lượt · đổi tên thành `p95` trần · không ghi `luotHong`. |
| TB-P02 | Tối ưu dựa trên bottleneck đã đo | P2 | P01 | **DONE** | **Xếp hạng bottleneck trước, sửa sau.** Bottleneck LỚN NHẤT nằm **ngoài tay đội**: retry của RPC công cộng — 11/30 lượt gọi >7 lần RPC, trung vị **2875 ms** so với **852 ms**, chênh **3,4×**, Pearson **r = 0,84**. Nói ra vì nó quyết định phần còn lại: mọi thứ đội tối ưu được cộng lại vẫn nhỏ hơn dao động của một endpoint công cộng. **Đã đổi — dùng lại dữ liệu đã trả tiền để lấy:** `fetch.ts` đọc tài khoản mint của những mint KHÔNG nằm trong `allKeys` bằng một lượt RPC riêng, lấy `decimals`, rồi **vứt `AccountInfo` đi**; phần ký hiệu token ngay dưới dựng `duLieuMint` bằng `allKeys.findIndex(...)` nên với đúng nhóm mint đó nó **luôn** trả `-1` ⇒ `null`. Đo trên fixture: **14/14** mẫu cho `0/1` (và `0/2`) mint thiếu nằm trong `allKeys` — con đường Token-2022 *"metadata nằm ngay trong tài khoản mint, không tốn lượt gọi nào"* **chưa bao giờ chạy được** cho nhóm mint mà repo vừa bỏ một lượt RPC ra đọc. **Lợi ích kép, vế thứ hai quan trọng hơn:** 93 → **81** lượt RPC trên 19 fixture (**−12,9 %**, trung bình 4,89 → 4,26/lượt kiểm), và mint Token-2022 nay đọc được ký hiệu ngay thay vì rơi xuống nhánh PDA Metaplex nơi nó thường không có metadata ⇒ bảng chênh lệch hiện tên token thay vì `Agsm…Lf4Z`. **Thời gian:** ~53 ms/lượt (RTT đo devnet, n=7) = **6,2 %** của lượt không retry, **3,9 %** của trung vị chung — nhỏ so với retry, nhưng là phần đội **kiểm soát được**. **Chi phí:** một `Map` sống đúng trong phạm vi một lần `inspect()` — **không phải cache**, thẻ cấm cache balances/authority/verdict. **Thay đổi thứ hai:** adapter Anthropic truyền `maxRetries` **tường minh** (`RETRY_MAC_DINH = 2`), đóng đúng việc `NGAN-SACH-RPC.md` mục 4 ghi là *"chưa làm"*. **Giữ nguyên giá trị** — hạ xuống 0 làm con số chi phí đẹp hơn và làm sản phẩm kém chịu lỗi hơn đúng lúc nhà cung cấp trả 429; guard canh **cả hai chiều**. **Đã cân nhắc và CỐ Ý KHÔNG làm:** cache (thẻ cấm) · gộp gMAI #1+#2 (không gộp được — #2 chỉ biết hỏi gì sau khi #1 và `simulateTransaction` trả về) · `disableRetryOnRateLimit` (sai hướng) · lazy-load web3.js 346 KB (ví và trang phỏng vấn đều gọi `inspect()` thật nên lazy chỉ **dời** chi phí; trang số liệu đã không kéo nó) · hạ `MAX_VI_TRA`/`HAN_LAM_GIAU_MS` (ba chặng làm giàu đã có `Promise.all` + hạn 2500 ms, không nằm trên đường găng) · `AbortSignal` (cần ADR, mục 5). **Không hồi quy — đo, không hứa:** replay **19/29 đạt · 0 hỏng** trước và sau; `so-baseline` lệch kỳ vọng người gán **0/5** ca, chạy lại SAU khi sửa chứ không chép số lượt trước. Guard `toiUuP02.test.ts` 4 bài, **mutation 4 hướng đều đỏ**. Quyết định đầy đủ: `NGAN-SACH-RPC.md` mục 7. |
| TB-L01 | Sửa cách đo và diễn đạt kết quả AI **[T04]** | P2 | C04, G02 | **DONE** | Tách một trường thành ba: `duongDoCoBocThoiHan` (false) · `soLuotUocSeBiCat` (**ước lượng**, 1–4/38 tuỳ lượt) · `soLuotBiCatThatSu` (**quan sát**, 0 — vì đường đo không bọc `boiThoiHan`). Đổi tên là một phần bản sửa: tên cũ `soLuotVuotHanMacDinh` bị đọc thành số quan sát. Bỏ hẳn việc **đếm lượt đo bằng tay** trong tài liệu (nguồn gốc mâu thuẫn 7 vs 8 lượt) — guard `t04Claim.test.ts` canh. Số bất lợi giữ nguyên; **không** đổi model, không mở dịch vụ có phí. |
| TB-L02 | Thử cải thiện AI có giả thuyết đo được | P2 | L01 | **có điều kiện** | Chỉ làm khi có mục tiêu đo được cụ thể. |
| TB-D01 | Một nguồn cho claim hiện hành **[T04]** | P2 | L01 | **DONE** | 451 → **520** qua `npm run so-lieu`; deck và release notes sinh lại (hai guard số test đã bắt được, đúng việc của chúng). `NGHIEM-THU-V01.md` gắn ngày cho câu *“không còn lỗi đã xác nhận”* và liệt kê bốn lỗi tìm ra **sau** nó. Guard mới `t04Claim.test.ts` 5 bài — **đã kiểm phủ định**, và bài đầu lúc viết KHÔNG đỏ được vì regex dựng theo trí nhớ về định dạng (`**487 pass` trong khi dòng thật là `**Bộ test: 487 pass`); đây là lần thứ năm cùng hình dạng lỗi đó. |
| TB-D02 | Pitch Technical và bộ câu hỏi phản biện | P2 | D01, X03, I02 | DONE | Pitch Technical, 11 phản biện, deck 12 slide; đã render PowerPoint và kiểm schema. Thu hẹp claim trace 2/14 luật. |
| TB-D03 | Video demo và phương án mất mạng | P2 | D02, B07 | DONE | MP4 khoảng 80 giây có caption; nguy hiểm, đối chứng, trace/rule và RPC fault injection. Có video dự phòng offline. |
| TB-V01 | Nghiệm thu Technical trên bản ứng viên | P1 | C01–C06, S01–S03, B01–B07, X01–X03, I01–I03, P01–P02, L01, D01 | **DONE (nguồn sạch, phần local)** | **Điều kiện chặn của bản PARTIAL đã hết:** lượt live chạy lại trên cây SẠCH (`c459ba7`, `inspect()` 574 ms), artifact không còn `dirtyWorktree`. Cổng `kiem-san-pham`: **11 đạt · 0 hỏng · 0 chưa rõ** — kể cả ô *"Lượt live gần nhất"* và *"Accessibility bản hiện tại"* vốn là hai ô khó nhất vì chúng so **dấu vết nội dung**, không so SHA. **Phải chạy lại 8 probe UI** vì ba commit đổi giao diện (`ac69a73` animation LED · `ec39cb3` branding · `8ceb8ab` nền trang) làm dấu vết đổi `c90436fd → 7c7e5a44`, số file 30 → 35; bằng chứng cũ nói về một bản dựng khác. Kết quả trên giao diện mới: axe toàn PASS, vùng bấm **26/26** đạt 44px, bàn phím/phóng to **16/16**, và **0 animation còn chạy** khi bật `prefers-reduced-motion` — animation mới vẫn tôn trọng thiết lập đó. **Không đóng phần remote:** CI trên revision ứng viên vẫn ở TB-H02, chưa push nên chưa có run thật; repo **không** giả một URL run. Ghi theo đúng quy tắc trạng thái của TB-I03 — local và remote không chung một ô. |
| TB-D04 | Gói nộp có thể chạy và review độc lập | P2 | V01, D03 | DONE — gói review | CUSTOS-REVIEW.zip có demo độc lập, deck, video, hướng dẫn và SHA-256. Vì V01 PARTIAL, chưa gọi release candidate. |
| TB-V02 | Chấm lại, báo cáo khoảng trống và bàn giao | P2 | D04 | DONE | Báo cáo codex-20260916/BAO-CAO.md: 7,75/10 có điều kiện, bốn kết luận riêng; không ước lượng xác suất giải. |
| TB-H01 | Xác nhận cách áp rubric và trạng thái đăng ký | — | — | **WAIT_INPUT** | Cần BTC. Câu hỏi: mục 25% chấm một SDK off-chain không có contract riêng như thế nào? **Không** để nó chặn việc sửa code. |
| TB-H02 | Quyền và thao tác phát hành/nộp | — | — | **WAIT_INPUT** | Cần quyền của chủ dự án cho push/publish/deploy/nộp. |
| TB-O01 | Môi trường thực thi Solana cục bộ | — | nền bắt buộc | **có điều kiện** | Chỉ sau khi phần bắt buộc đủ ổn định. |
| TB-O02 | Decoder hoặc extension mới theo khoảng trống đo được | — | B06, S02 | **có điều kiện** | Chỉ khi phép đo chỉ ra khoảng trống thật. |
| TB-O03 | Migration dependency/runtime lớn | — | S03 | **có điều kiện** | |

## Bảng công việc — roadmap trước

Quy ước: TODO, DOING, VERIFY, DONE, WAIT_INPUT, NOT_NEEDED theo định nghĩa trong roadmap. Cột bằng chứng phải có liên kết khi DONE hoặc NOT_NEEDED. Với WAIT_INPUT, ghi vào bảng trở ngại phía dưới và tiếp tục việc khác đủ phụ thuộc.

| Mã | Việc | Vai | Phụ thuộc | Trạng thái | Bằng chứng / ghi chú |
|---|---|---|---|---|---|
| R00 | Baseline, quyền và đối chiếu lỗi | A/B/D | — | DONE | HEAD `55388d6` lúc bắt đầu · Node 24.12.0 · npm 11.6.2 · Chromium 149.0.7827.55 · Playwright 1.61.0 · axe-core 4.13.0. Tái hiện F09 trên PowerShell (402/403). Đối chiếu F01–F11 ở BAN-GIAO.md. |
| R01 | Kiểm deck không phụ thuộc ngầm vào unzip | A/D | R00 | DONE | `e457eef`. `scripts/docZip.ts` đọc ZIP bằng `node:zlib`, không tiến trình con. PowerShell không có `unzip` → 403/403. Deck cũ vẫn làm test đỏ. **Chưa kiểm Linux** trong phiên này. |
| R02 | Cổng kiểm chứng và nguồn gốc artifact | A/D | R00, R01 | DONE | `dauVetNoiDung()` băm nội dung mã/giao diện từ cây làm việc; cổng so dấu vết TRƯỚC phả hệ commit. Tái hiện lỗ hổng: sửa `style.css` không commit thì ô a11y vẫn báo `✓`; nay báo `~ nội dung mã đã đổi sau lượt đo`. 7 bài trên kho git tạm. |
| U01 | Kết quả mobile và điều hướng focus | B | R00 | DONE | `a715ee1`. Đo lại 320/375/768 px: top=9/9/154, focus vào khối kết quả ở cả ba. `scripts/kiem-trinh-duyet/soi-ket-qua-trong-tam-nhin.py`. axe 40/40 sau thay đổi. |
| U02 | Ký/gửi/xác nhận và chống gửi lặp | B/A | R00 | DONE | `5dbc6ae`. Sáu pha; `chuaRo` tách khỏi `thatBai` khi đã có chữ ký. Luồng ở `src/gui.ts`, 5 bài kiểm bằng stub trong `npm run check` — ký thật đòi khoá nên logic trong component gần như không ai kiểm. Bản công khai vẫn không ký. |
| U03 | Validate hiện trường, phục hồi render | B/A | R00 | DONE | `4ffe088`. Xác thực từng trường thay cho `as HienTruong`; ba trạng thái co/chuaDung/hong. 7 bài đơn vị + 5 ca trình duyệt (`soi-cau-hinh-hong.py`): không ca nào trắng trang, không ca nào tạo nút Ký. |
| U04 | Phản hồi payload dApp không hợp lệ | B/A | R00, U03 | DONE | `0b8daba`. Union `khong`/`co`/`hong` + giới hạn 4096 ký tự. 7 bài đơn vị; trình duyệt: payload hỏng không sinh khối kết quả, không hiện nhãn phán quyết nào. |
| U05 | Xác nhận huỷ đúng trạng thái | B/C | U01, U02 | DONE | `0b8daba`. `role=status`: "chưa được gửi và sẽ không được gửi", chỉ nói về yêu cầu hiện tại. |
| U06 | Vùng bấm, câu chữ và phân cấp | B/C | U01, U05 | DONE | F08. Vùng bấm chuyển vào lớp `.lien-ket` thay vì vá bốn nơi gọi; 26/26 đạt 44px. Chi tiết implementation AI/khoá xuống mục kỹ thuật, giữ lại câu bảo vệ quyết định số 1. Header nói rõ là ví mẫu tích hợp SDK. Bàn phím · zoom · chữ dài: 16/16. axe 40/40 chạy lại. |
| U07 | Cấu hình RPC và handoff nhất quán | B/A | U03, U04 | DONE | KHÔNG phải `NOT_NEEDED`: trang tấn công gọi thẳng `ht.rpc` ở 2 chỗ (bỏ qua `VITE_RPC`, không fallback), và địa chỉ ví hỏi TÊN MÁY nên vào bằng `127.0.0.1`/`[::1]`/IP LAN thì giải ra **chính trang tấn công**. Chính sách gom về `scripts/diaChiDemo.ts`; 8 bài đơn vị + `soi-handoff.py` 5/5. |
| I01 | Deadline/retry trang phỏng vấn | B | R00, U03 | DONE | `1a94356`. Hạn 15 s bọc cả chuỗi, dùng lại `scripts/coHan.ts`. Treo RPC thật: dừng sau 15,3 s, có nút Thử lại. |
| I02 | Lưu, validate và khôi phục phỏng vấn | B/D | R00 | DONE | `3cc77be`. Kho hỏng được GIỮ nguyên văn + nút tải bản sao; bắt lỗi ghi storage. 8 bài đơn vị, 4 ca trình duyệt. |
| I03 | Công cụ khớp giao thức nghiên cứu | B/C/D | I01, I02, U06 | DONE | Thêm `hieuCoverage`, `docNhamPhi`, `kenh`, `lyDoQuyetDinh` — tất cả TUỲ CHỌN nên 20 bản ghi vòng 1 vẫn đọc được. Bộ đếm lấy mẫu số là số người ĐƯỢC HỎI, không phải tổng bản ghi. Nút thẻ cảnh báo hết no-op: ghi `bamThat` (tách khỏi `quyetDinh`) và nói rõ không có giao dịch nào được gửi. 7 bài đơn vị + `soi-phong-van-vong-2.py` 15/15. |
| S01 | Phân loại advisory theo phơi nhiễm | A | R00 | DONE | F11. Tra registry 08/09: cả ba gói trực tiếp đã ở bản mới nhất, và **không advisory high nào có bản đã vá** — `image-size` mới nhất `2.0.2` vẫn nằm trong dải `<=2.0.2`. Chờ vá không phải kế hoạch. Bốn điều kiện chấp nhận rủi ro thành test (`phoiNhiemPhuThuoc.test.ts`), có kiểm phủ định. Người chịu trách nhiệm và ba mốc xem lại ghi ở `PHU-THUOC.md` mục 3.5. |
| S02 | Vá tương thích và xử lý rủi ro còn lại | A/B | S01, R01 | TODO | Có thể WAIT_INPUT riêng cho quyết định chưa được giao. |
| S03 | Kiểm SDK từ consumer ngoài repo | A/C | R01 | DONE | Tarball + JS + `tsc` + optional peer dep đã có sẵn. Sửa **đối chứng dương vô dụng**: nó gửi vào chính `moc.explanation` nên bộ chắn vứt sạch cũng cho cùng kết quả. Mutation chứng minh: tắt hẳn lớp mô hình thì 10/10 bẫy VẪN xanh, chỉ đối chứng mới bắt được. Thêm ca L3 ném lỗi và L3 treo (hạn 300 ms, lui sau 301 ms). Local và registry `0.2.0` khớp nhau. |
| D01 | Đồng bộ claim và tài liệu hiện hành | D/C/A | R02 | DONE | F10. Bảng claim ở `docs/BANG-CLAIM.md`. Ba lệch số (29→30 dòng, 6/6→13/13 bẫy, 33→38 mẫu) đưa vào generator + danh sách mốc. Ba câu thu hẹp: trần cứng 400 token, thị trường chứng minh hộ, "ví hiện tại cho họ xem". |
| D02 | Benchmark có nhãn, tập giữ lại | A/D | R02 | DONE | `docs/BENCHMARK.md`. **Không** báo confusion matrix: nhãn `kyVong` do chính đội gắn cùng lúc viết luật nên vòng tròn — ghi rõ cần gì để có nhãn độc lập. Tập giữ lại: thư mục trống + quy tắc 4 bước + guard (`giuLai.test.ts`); cắt mẫu cũ ra gọi là holdout là đặt tên mới cho dữ liệu đã dùng. Lý do bỏ mẫu tách hạ tầng/sản phẩm, ghi từng chữ ký. Đo lại 08/09: 4/20 (25/08: 9/20), **16 hạ tầng · 0 sản phẩm**. |
| D03 | Số đo độ trễ và tối ưu có căn cứ | B/A | R02, U01, U02, U07 | DONE | `docs/HIEU-NANG.md` + `soi-do-tre.py`, đo trên bản **production** chứ không dev server. Tải trang FCP 116 ms · 173 KB qua dây; bấm→kết quả trung vị ~850 ms, cao nhất 6386 ms; 7 lượt RPC (cao nhất 17). Điểm ngoại lai là **retry**, không phải khởi động nguội — ghép cặp ms/RPC mới thấy. Tìm ra `npm run preview` phục vụ trang TRẮNG với mã 200. CHƯA tối ưu: nút thắt vừa xác định. |
| A01 | Eval offline, guardrail và tooling | C/A/D | R02, D02 | DONE | Thêm **đối chứng dương 3/3** — 13/13 một mình không phân biệt được bộ chắn hoạt động với bộ chắn vứt sạch. Lượt offline không còn xoá lượt live (`liveGanNhat`); lỗi cũ ĐÃ xảy ra thật. Bản ghi live thêm prompt hash, dataset version, maxTokens/maxRetries, lỗi API, và **số lần lui về câu tất định**. Model đọc từ hằng số, không gõ tay. |
| A02 | Eval mô hình thật trong ngân sách | C/D | A01 | **DONE 12/09** | 7 lượt live, `claude-haiku-4-5`, 38 mẫu. 760 token vào / 184 ra mỗi lượt. **Kết luận có lợi ích: KHÔNG đo được lợi ích** — trên thước nêu-coverage mô hình 13–14/16 (8 lượt, dao động), câu mẫu 14/16: ngang, không hơn. Xem `docs/DON-VI-KINH-TE.md` mục 3. |
| B01 | Bộ làm việc với người mua | D | R00 | DONE | Bộ đồ nghề đã có ICP, 6 câu, mẫu tin nhắn, schema và validator. Thêm phần **lấy 30–50 cái tên ở đâu** — năm nguồn công khai + ba câu lọc + bảng theo dõi; cố ý KHÔNG điền sẵn tên tổ chức. Thêm `nguoi-mua.example.json` (cờ `laViDu`). Sửa cửa quyết định gõ cứng một ngày cụ thể → phát biểu theo sự kiện, và mở rộng guard lịch: quét MỌI `.md` thay vì bốn file. |
| B02 | Bộ tự tích hợp cho đối tác | A/D | S03 | DONE | `docs/PILOT-TU-LAM.md`, viết SAU khi chạy thật từ thư mục trống với gói registry (core 0.1.1 · ai 0.2.0 · web3 1.99.0). Ghi hai bẫy đo được: `npm init -y` không đặt `type: module`, và lượt đầu ra `warning` vì ví mới chưa có SOL — fail-safe, không phải lỗi. Có ranh giới cưỡng chế (ví tin cậy, không phải dApp), bốn đường lỗi, cách gửi lỗi đã lọc secret, mẫu biên bản. 3 bài guard chặn tài liệu dạy import không tồn tại. |
| B03 | Bộ usability cho UI hiện tại | B/C/D | I03, U06 | **DEFERRED_SCOPE** | Cần người tham gia mới. Hoãn theo mục 1.3 roadmap Technical — **không phải đã làm**, cũng không phải bị chặn kỹ thuật. Bộ giao thức đã khoá vẫn giữ nguyên trong repo. |
| B04 | Mô hình doanh thu và giả thuyết giá | D | B01, B02 | DONE | `docs/MO-HINH-DOANH-THU.md`. MIT nên core BUỘC miễn phí — bán thứ không fork được: luật cập nhật, người chịu trách nhiệm, SLA. Chi phí biên $0,0000325/lượt nên giá neo vào GIỜ NGƯỜI, không vào lượt gọi. Ba gói, cả ba khai giả định, mỗi cái kèm phép thử. Có mục **điều kiện mô hình KHÔNG khả thi** (5 điều kiện) và dám kết luận «không là một doanh nghiệp». 5 bài guard. |
| P01 | Pitch, deck và phản biện | D/C | D01, B04, U06 | DONE | Thêm **slide mô hình doanh thu** — deck có slide thị trường và slide chi phí nhưng không slide nào nói ai trả/trả cho gì, tức ô 25 % chỉ trả lời một nửa. Sửa câu Q&A về doanh thu: bản cũ nói «trả theo lượt kiểm tra», MÂU THUẪN với chính phép đo ($0,0000325/lượt). Thêm bản nói cho cả ba định dạng thể lệ (4+2+1 · 5+2 · 5+3). 6 bài guard deck: slide rỗng, mojibake, placeholder, số lệch. |
| P02 | Video thật và phương án mất mạng | B/D | U01, U02, U03, U04, U05, U06, U07, P01 | TODO | Có thể ghi màn hình bằng công cụ nếu đủ điều kiện. |
| P03 | Đóng gói bản nộp cục bộ | B/D/A | D01, S03, P01 | DONE | Đóng gói chạy được; chép ra ngoài repo, phục vụ bằng `phuc-vu.mjs`, mở cả 4 trang: 12/12 PASS, 0 lỗi console, 0 tài nguyên 404. Thêm bước 4/4 vào script: soi đường dẫn cá nhân + đủ trang. **Guard đó lúc đầu KHÔNG BAO GIỜ nổ được** — regex mất một dấu `\` nên khớp `C:Users|`; chỉ kiểm phủ định mới lộ. Tag/publish/deploy vẫn là bản nháp — CHỜ CHỦ DỰ ÁN. |
| V01 | Nghiệm thu sản phẩm trên bản cuối | A/B/C | (xem roadmap) | DONE (phần khả dụng) | `docs/NGHIEM-THU-V01.md`. Ma trận 10 bề mặt chạy thật trên cùng bản mã: engine 14/14 · a11y 40/40 · bàn phím 16/16 · vùng bấm 26/26 · handoff 5/5 · phỏng vấn 15/15 · SDK 10/10+3/3 · bộ mang đi 12/12. Cổng: check 478/478 · kiem-san-pham 11/0/0. **KHÔNG đóng toàn diện** — S02 không tự thông được. Phạm vi Chromium giả lập, đã ghi rõ. Sửa ô [máy] cuối của `nop-bai-strict`. |
| V02 | Bàn giao, checklist và chấm lại | D/A | R00 | DONE (phần lập báo cáo) | `docs/NGHIEM-THU-VA-BAN-GIAO.md`. Bốn nhóm kết luận tách riêng; checklist đọc từng mục (7/10). KHÔNG chấm lại 6,95 của `CUSTOS.md` — chỉ ghi lý do trừ điểm nào còn đúng, lý do nào hết hiệu lực. Ô trình bày ghi TẠM CHẤM vì chưa ai thấy đội trình bày. 5 bài guard chống trôi một chiều. |
| H01 | Thu và phân tích usability thật | D/người tham gia | B03 | **DEFERRED_SCOPE** | Chờ người thật. Hoãn theo mục 1.3; dữ liệu 20 cuộc vòng 1 giữ nguyên, **không** đánh dấu đã hoàn thành. |
| H02 | Phản hồi người quyết định mua | D/người mua | B01 | **DEFERRED_SCOPE** | Nhóm không thể phỏng vấn kỳ này. Hoãn theo mục 1.3 — bộ đồ nghề B01 vẫn dùng được khi có điều kiện. |
| H03 | Đối tác tự thử tích hợp | A/D/đối tác | B02 | **DEFERRED_SCOPE** | Cần bên thứ ba thật; Claude tự thử **không** tính là bằng chứng độc lập. Hoãn theo mục 1.3. |
| H04 | Xác nhận các mục BTC còn thiếu | D/chủ dự án/BTC | R00 | TODO | Chuẩn bị câu hỏi; không tự gửi khi chưa được giao. |

## Trở ngại và điều kiện mở lại

Chưa điều tra trong lần thực thi roadmap. Những đầu vào ngoài repo dự kiến cần kiểm là A02, H01–H04; không mặc định đã có hoặc chắc chắn không có.

| Mã việc | Thiếu chính xác điều gì | Tài liệu/artifact đã chuẩn bị | Ai/cái gì mở lại được | Việc độc lập làm tiếp |
|---|---|---|---|---|
| — | Chưa ghi nhận trong phiên triển khai | — | — | R00 |

## Nhật ký nghiệm thu

Chưa có việc nào được nghiệm thu theo roadmap này. Khi có kết quả, thêm một bản ghi cho mỗi việc:

```text
Mã:
DONE / NOT_NEEDED / WAIT_INPUT:
Nguyên nhân hoặc quyết định:
File thay đổi:
HEAD và dấu vết working tree:
Lệnh / exit code / kết quả:
Đường dẫn bằng chứng:
Phạm vi chưa kiểm:
Điều kiện mở lại khi mã hoặc dữ liệu đổi:
```

## Quy tắc cập nhật

- Cập nhật bảng công việc khi bắt đầu, khi chờ đầu vào và sau kiểm chứng; không chỉ sửa đoạn tổng kết.
- Khi thay đổi làm bằng chứng cũ hết hiệu lực, đưa việc bị ảnh hưởng về VERIFY và ghi lý do, không chạy lại mọi thứ không liên quan.
- Khi đã có báo cáo baseline mới, dẫn artifact đó; không sao chép một con số sang nhiều ô trạng thái.
- Không dùng tổng phần trăm hoàn thành để gọi sản phẩm đã sẵn sàng: bốn việc H và nhánh A02 có ý nghĩa khác với sửa một lỗi UI.
- Không tự tạo commit để đổi cây làm việc thành sạch. Liệt kê thay đổi cần chủ dự án review ở sổ bàn giao.
